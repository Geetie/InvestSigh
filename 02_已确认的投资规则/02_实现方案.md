# 第二章《已确认的投资规则》实现方案

> **章性质**：第二章是**硬约束层（最高优先级）**。产物不是新功能，而是 **① 禁止项清单的可自动检查（P-01~P-10）② 缺失字段补齐（A-02）③ 前置门强制必填（A-06）④ 规则版本 + 变更审计**。
> **实现策略**：**约束层增量**——用"schema 断言 + AST/配置扫描 + 决策输入结构断言 + 展示规则检查"四类检查把红线固化，**并在本章修复 PM 约束回溯查出的 2 条硬冲突**。
> **依赖**：默认第九章、第六~七章、第四~五章实现方案已存在；本章只引用其对象/函数，不重复其内容。
> **覆盖**：第二章 23 条需求（P0 21 / P1 2）——全部落位；含 **A-02、A-06 两条硬冲突的修复设计**与**回改清单**。
> **全局红线**（原文第 33 行）：技术实现、信息筛选、**页面设计**三层均不得悄悄增设与约定冲突的买卖条件。
> **版本**：v1.1（2026-09-15 回改：R-01~R-37 全部执行，见 §F「执行状态」列；其中 **R-26~R-34 为收口轮回改**、**R-35~R-36 为收口后新增件**、**R-37 为环境迁移（工程根 Windows→macOS）**；技术待定项 ① 关闭，见文末；R-25 覆盖矩阵口径归正，明细见文末「附：回改记录」）

---

## A. 本章定位与实现总览

第二章把 14 条投资规则翻译为**可自动检查的约束**，核心是三件事：

```
① 禁止项自动扫描（P-01~P-10）  ←  本章最核心落地物
② 字段补齐（opportunity_types[] / moat_level）  ←  含硬冲突 A-02 修复
③ 前置门强制（提前判断三要素 / horizon 区间 / 反拖延）  ←  含硬冲突 A-06 修复
        ↓
四类检查机制：schema断言 · AST/配置扫描 · 决策输入结构断言 · 展示规则检查
        ↓
统一由「冲突扫描器 conflict_scan」在 4 个时机自动运行（§B.3）
```

**关键设计判断**：
- **为什么不用运行时"检查报告"而用"断言/门"**？因为第二章条款是**首版硬验收**，"提示后可忽略"等于没有条款。故：字段级 → `schema.assert_*`；函数/配置级 → AST 扫描**不过即 fail**；决策级 → gate 输入结构断言。
- **为什么"排序 ≠ 门槛"**（C123-4）？排序是**处理顺序**（Ch6 `priority_order` 字典序），**不拒绝任何建议**；门槛是"能否出建议"。二者作用环节不同，本章在 §B.2 明确"排序环节允许、判定环节禁止"。

---

## B. 禁止项 P-01~P-10 自动化检查设计（核心）

### B.1 检查口径（五层）

| 层 | 检查方式 | 覆盖禁止项 |
|---|---|---|
| L1 schema 字段存在性断言 | `schema.assert_absent(fields)` / `assert_present(fields)`；**作用域**：断言对象是 **`Recommendation` 对象的 `properties` 字段名集合**，**非全仓 token 扫描**（见下方「★ 禁词作用域通则」） | P-03 P-04 P-05 P-07 |
| L2 AST + 禁词表扫描 | 解析 `scripts/**/*.py`，命中禁词即 fail；**P-09 例外**：`weight`/`score` 等通用词仅在**决策函数**内触发（`is_decision_scope` 的**纳入 / 排除三类调整**见 §B.3 Checker-1） | P-01 P-02 P-09 |
| L3 配置扫描 | 遍历 `rules/**/*.{yaml,json}` 键名/值；**扫描范围含第八 / 十 / 十一章新增配置**（清单见 §B.4「被检目标清单（含新层配置）」） | P-01 P-02 |
| L4 决策函数输入结构断言 | 对 gate 的输入 dataclass 做字段集合断言 | P-06 |
| L5 展示层渲染规则检查 | 渲染配置/组件排序·颜色·折叠策略断言 | P-10 |

> **★ 禁词作用域通则（P-09 / P-04 同族，通用于全部禁词）**：
> 1. **入库前必查同名**：任一禁词写入 `rules/banned_tokens.yaml` 之前，**必须先在包内 `grep` 该 token**，确认是否存在**合法同名用途**（不同语义、不同域的同拼写 token）。
> 2. **有同名必须声明 `scope`**：若存在**合法同名用途**，该 token **必须**声明 **token 级 `scope`**（落 `rules/banned_tokens.yaml`，随 BANNED_TOKENS 一同版本化 + SHA256 锁）；**无 `scope` 不得入库**（否则在全仓扫描下必与合法同名词碰撞，而本包定「命中即 fail、禁 warn-only」，一次误报即卡死发布流程）。
> 3. **scope 必须可执行**：scope 判定必须是**函数级 / 对象级**的**可执行断言**（如"仅在决策函数内生效""仅对 `Recommendation` 字段名生效"），**不得写成"人工注意"**之类不可检查的措辞。
> 4. **本通则的三次实例**：**P-09**（`weight`/`score`/`vote`/`sentiment`/`follower_count` —— 合法同名的大量普通变量，scope = 仅决策函数内，见 §B.3 Checker-1）；**P-04**（裸词 `position` —— 合法同名词 `position_listed`/`business_positions` 表示"产业链位置"语义，scope = 仅 `Recommendation` 字段名，见 §B.3 Checker-2）；**检查器命名空间免扫**（**检查器自身的函数名 / 变量名可能含禁词**，如 `assert_no_composite_score` / `check_no_weight`，朴素扫描会**误报**；scope = **仅在 `scripts/checks/**` 与 `tests/**` 的检查器命名空间内免扫**）。**三次实例只做"消歧"，不新增禁止项、不放松约束**（同 P-04 / P-09 一类）。

### B.2 ★禁止项清单（可直接落地为自动化检查）

| 编号 | 禁止项（禁词/禁结构） | 检查方式 | 对应规则 | 落地文件 |
|---|---|---|---|---|
| P-01 | `min_excess_return` / `minimum_alpha` / `safety_margin` / `excess_threshold` | L2+L3 | 规则9 | `scripts/checks/conflict_scan.py` |
| P-02 | `confidence_threshold` / `min_confidence` / `confidence_gate` | L2+L3 | N2.3-02 | 同上 |
| P-03 | `stop_loss` / `stop_price` / `price_stop` | L1 | 规则11 | `tests/conflict/test_schema_no_stop.py` |
| P-04 | `quantity` / `position` / `shares_held` / `cost_basis` / `portfolio_weight` | L1（仅 `Recommendation` 字段名断言；**P-04 约束的是"建议对象上不得出现仓位/持仓/成本类字段"，不约束表示"产业链位置"语义的 `position_listed` / `business_positions`**，见「★ 禁词作用域通则」） | 规则3 | 同上 |
| P-05 | `short` / `short_sell` 作为 `action` 值 | L1（枚举断言） | N2.2-06 | 同上 |
| P-06 | `moat_required` / `catalyst_required` / `official_confirmation_required` / `profitability_required` 作买入门 | L4 | 规则4/5/7/12 | `scripts/checks/assert_gate_input.py` |
| P-07 | 无 `source_id` 的聚合预测（`consensus`/`market_view`/`average_forecast`） | L1 | 承 Ch5 C.2 | `tests/conflict/test_schema_no_aggregate.py` |
| P-08 | 依据 `past_return` 的收益预测 | 程序检查 | 承 Ch3 AGIX | Ch5 `history_guard`（引用） |
| P-09 | `weight`/`score`/`vote`/`sentiment`/`follower_count` 进入决策函数 | L2（`scope` 见 Checker-1）：**纳入**通知 / 每日页排序函数（排序统一走 `priority_order` 字典序，禁 `weight`/`score`）；**排除**复盘标记字段 `eval_layer`/`error_axis`/`error_axis_note` 与 `freeze_param` 读取（均为配置 / 标记，非决策输入） | 承 Ch6 B.3 | `scripts/checks/conflict_scan.py` |
| P-10 | 展示层"买入置顶/紧迫色/隐藏反证"等隐性引导 | L5 | N2.3-01 | `scripts/views/neutrality_check.py` |

### B.3 检查器伪代码（≥3 个，给出 5 个）

> 均为语言无关伪代码，工程师可落地为 Python。**核心约束：命中即 fail（非零退出码），禁止 warn-only。**

```python
# ── Checker-1：AST + 禁词表扫描（覆盖 P-01 / P-02 / P-09）────────────
# scope（★设计约束，消歧 P-09 误报）：P-01/P-02 为**全局**扫描；P-09 的通用词
#   （weight/score/vote/sentiment/follower_count）**仅在决策函数内**命中才 fail——
#   否则业务代码里的普通 "score/weight" 变量会误报，在"命中即 fail、禁 warn-only"
#   下形成死锁。判定"是否决策函数"（is_decision_scope）= 位于 decision/gate 模块或
#   classify_*/assert_* 调用链（对应 §B.2 P-09"进入决策函数"的口径）。
#   scope 的四类调整（方向不同，勿合并）：
#     · 纳入：**通知 / 每日页排序函数**属决策作用域（排序统一走 priority_order 字典序，
#       禁 weight/score）—— N8.1-07 / N8.1-09。
#     · 排除：复盘**标记字段** eval_layer / error_axis / error_axis_note **不在**决策作用域内
#       （标记字段不得进入 decision/graph 作用域）—— N10.3-12。
#     · 排除：freeze_param 读取**不在**决策函数 scope 内（参数为配置 / 标记，非决策输入）—— N11.2-12。
#     · 免扫：检查器命名空间 scripts/checks/** 与 tests/** 免扫（检查器自身命名含禁词属正常）—— 见 §B.1 通则第 4 点。
BANNED_TOKENS = load_yaml("rules/banned_tokens.yaml")  # 只读、版本化、SHA256 锁
SCOPED_TOKENS = {"weight", "score", "vote", "sentiment", "follower_count"}  # 仅决策函数内生效

def scan_ast(root: str = "scripts") -> list[Violation]:
    violations = []
    for path in walk_py(root):
        if is_checker_namespace(path):                 # ★ 检查器命名空间免扫（见 §B.1 通则第 4 点「三次实例」）
            continue                                   # scripts/checks/** 与 tests/**：检查器自身函数/变量名可能含禁词，免扫以避误报
        tree = ast.parse(read(path))
        in_decision = is_decision_scope(path)          # scope 判定，见上方 ★设计约束
        for node in ast.walk(tree):
            # 变量名 / 函数名 / 属性名 / 字符串字面量 全量比对禁词
            for name in names_and_strings(node):
                tok = normalize(name)
                if tok in BANNED_TOKENS:
                    if tok in SCOPED_TOKENS and not in_decision:
                        continue                        # P-09 通用词：非决策函数内不触发
                    violations.append(Violation(
                        rule=map_to_rule(name), file=path, node=node.lineno,
                        severity="FATAL"))
    return violations        # 非空 → 进程 exit(1)
```

```python
# ── Checker-2：schema 字段存在性断言（覆盖 P-03 / P-04 / P-05 / P-07）──
# ★ 作用域声明（P-04 消歧，对齐 §B.2「★ 禁词作用域通则」）：
#   本 Checker 的断言对象 `rec` **已被限定为** `schema["defs"]["Recommendation"]["properties"]`
#   ——即**仅该对象的字段名集合**。**禁止实现者把它扩展成全仓 token 遍历**：
#   `assert_absent(rec, ...)` 只对 `Recommendation` 的字段名生效，**不得**改成对
#   `scripts/**` 或全包文本做裸词 `position` 扫描。
#   反例（合法同名词，**不得误报**）：
#     · `position_listed`（"产业链位置"收录态，见 03 §需求拆解 N3.2-04 / 03 §实现方案 C.2）
#     · `business_positions`（公司×节点多业务位置数据对象，见 09 §2.1.3 / §3.3.3）
#   裸词 `position` 的禁止语义 = "建议对象上不得出现仓位/持仓/成本类字段"，
#   与"产业链位置"语义**无关**；朴素全仓正则会在 `position_listed` 上误报并卡死流程。
def assert_schema(schema: dict) -> None:
    rec = schema["defs"]["Recommendation"]["properties"]   # 作用域边界：仅此字段名集合
    # 必须"不存在"的字段（禁止项）
    assert_absent(rec, ["stop_loss", "stop_price",
                        "quantity", "position", "shares_held",
                        "cost_basis", "portfolio_weight",
                        "consensus", "market_view", "average_forecast"])
    # action 枚举不得含做空
    assert set(rec["action"]["enum"]) == {"buy", "sell", "pending", "maintain"}
    # 必须"存在"的字段（本章修复项，见 §C）
    assert_present(rec, ["opportunity_types", "horizon", "start_date"])
```

```python
# ── Checker-3：决策门输入结构断言（覆盖 P-06）────────────────────────
def assert_gate_input(gate_fn) -> None:
    fields = set(inspect.signature(gate_fn).parameters)  # 或读输入 dataclass 字段
    forbidden = {"moat_required", "catalyst_required",
                 "official_confirmation_required", "profitability_required",
                 "realization_required", "moat", "catalyst"}   # 均不得进门
    hit = fields & forbidden
    assert not hit, f"买入门含禁止输入: {hit}"   # 命中即 fail
    # 正向断言：门输入必须且只是"收益相对基准"的结构判定（C123-5：无置信度数值）
    assert_gate_has_no_numeric_confidence(gate_fn)
```

```python
# ── Checker-4：配置扫描（覆盖 P-01 / P-02）──────────────────────────
def scan_config(rules_dir: str = "rules") -> list[Violation]:
    hits = []
    for f in walk(rules_dir, exts=(".yaml", ".yml", ".json")):
        for key, val in flatten_keys(read(f)):     # 展开嵌套键
            if normalize(key) in BANNED_TOKENS:
                hits.append(Violation(rule=..., file=f, key=key, severity="FATAL"))
    return hits
```

```python
# ── Checker-5：展示层中性检查（覆盖 P-10 / 对齐 Ch3 N3.2-05）──────────
def check_view_neutrality(view_config: dict) -> list[Violation]:
    v = []
    # ① 默认排序不得以 buy 置顶
    if view_config["default_sort"][0]["field"] in ("action", "recommendation"):
        v.append(Violation("P-10", reason="默认排序按买入倾斜"))
    # ② 不得用紧迫色/涨跌色暗示
    if view_config.get("urgency_color") or view_config.get("red_green_bias"):
        v.append(Violation("P-10", reason="使用紧迫/涨跌色隐性引导"))
    # ③ 反证与缺口必须与支持证据同等可见（不得默认折叠）
    if view_config["evidence_panel"]["collapsed_default"]:
        v.append(Violation("P-10", reason="默认折叠反证/缺口"))
    # ④ 未完成研究不得渲染为"中性/无机会"（承 Ch3 N3.2-05）
    if any_render_path_maps(view_config, "未完成研究", to=("中性", "无投资机会")):
        v.append(Violation("N3.2-05", reason="未研究伪装中性"))
    return v
```

### B.4 自动运行机制（4 个时机）

> 所有 checker 汇总到 `scripts/checks/conflict_scan.py`（**聚合入口**），从 4 处自动触发；任一命中 → 非零退出 + 写 `reports/conflict_scan_<date>.json`。

| # | 时机 | 机制 | 作用 |
|---|---|---|---|
| 1 | **提交前** | Git `pre-commit` hook 调 `conflict_scan.py` | 防止违规字段/阈值进入仓库 |
| 2 | **CI / 推送** | GitHub Actions（经 `gh`）在 push 时全量跑 L1–L5 | 合并门禁；红线不可绕过 |
| 3 | **每日运行前置门** | 第八章每日跑第 7 步（发布）**之前**跑 L4+L5 | 防止"当天偷偷改口径/页面引导" |
| 4 | **夜间全量巡检** | WorkBuddy **Automation**（`rrule` 每日）跑全量 + 差异报告 | 兜底 + 变更审计 |

> **被检目标清单（含新层配置）**：Checker-4（L3 配置扫描）的扫描范围**不止既有 `rules/**/*.{yaml,json}`**，第八 / 十 / 十一章**新引入的配置一并纳入**，以守卫"不得把待定项补成新门槛"这条源稿红线在新层不失效：
> - `rules/trigger.yaml`（第八章）· `rules/notification.yaml`（第八章）· **三入口展示配置**（第八章）—— 触发 / 通知 / 展示层不得夹带门槛类键；
> - `rules/freeze.yaml`（第十 / 十一章，**含 `freeze_param[p10].caliber_terms`**）—— 冻结参数与口径术语仅作值 / 标记，不得升格为买卖条件；
> - `registry/delivery.yaml`（第十一章）—— 交付登记不得夹带门槛。
> **禁止项示例**（命中即 fail，防"借参数补门槛"）：`min_excess_return` / `stop_loss` / `confidence_threshold` 等（完整禁止项清单见 §B.2）。

```mermaid
flowchart TB
  subgraph SRC["被检目标"]
    A1["scripts/**/*.py"]:::t
    A2["rules/**/*.yaml"]:::t
    A3["schema (09 §1.1/§3.3.3)"]:::t
    A4["gate 输入结构 (07 §D.1)"]:::t
    A5["views/** 渲染配置"]:::t
    A6["rules/trigger.yaml · rules/notification.yaml (第八章)"]:::t
    A7["三入口展示配置 (第八章)"]:::t
    A8["rules/freeze.yaml 含 freeze_param[p10].caliber_terms (第十/十一章)"]:::t
    A9["registry/delivery.yaml (第十一章)"]:::t
  end
  SRC --> CS["conflict_scan.py (L1-L5 聚合)"]
  H1["① pre-commit"] --> CS
  H2["② CI (push)"] --> CS
  H3["③ 每日发布前置门"] --> CS
  H4["④ 夜间 Automation"] --> CS
  CS --> R{"命中禁止项?"}
  R -- 是 --> F["exit(1) + reports/conflict_scan_*.json + 审计"]:::bad
  R -- 否 --> OK["放行"]:::ok
  classDef t fill:#eef,stroke:#99c; classDef bad fill:#fee,stroke:#c66; classDef ok fill:#efe,stroke:#6c6;
```

---

## C. 字段增量与 schema 断言

> 章性质决定"只补字段、不动既有结构"。所有字段均遵守"追加式不可变 + 规则版本绑定"。

### C.1 【硬冲突 A-02 修复】机会类型字段（规则 2 / N2.1-02）

**问题**：五份实现方案**均无** `opportunity_type` 字段。
**修复（一次落位，落点在 Ch7 `recommendations`，Ch4/Ch5 提供输入）**：

| 字段 | 类型 | 位置 | 语义 |
|---|---|---|---|
| `opportunity_types[]` | array<enum> | `recommendations` | `A`（基本面改善且价格未充分反映）/ `B`（基本面未恶化但价格有吸引力）；**数组、不互斥、可同时为空或并存** |
| `opportunity_basis[]` | array<object> | 同上 | 每类一条依据：`{type, value_ref, price_ref}`，分别指向 Ch4 价值状态与 Ch5 价格缺口 |
| `value_state_refs` | object | Ch4 价值层输出 | A 类依据来源（`growth_momentum_state`/`certainty_state`/`moat_level`/`moat_state`） |
| `price_gap_state` | enum | Ch5 价格层输出 | `fully_reflected / not_fully_reflected / attractive`；A/B 分类的价格维输入 |

> **分类函数**（新增，放 Ch7 决策层）：
> ```python
> def classify_opportunity(value_state_refs, price_gap_state) -> list[str]:
>     out = []
>     if is_improving(value_state_refs) and price_gap_state == "not_fully_reflected":
>         out.append("A")
>     if not is_deteriorating(value_state_refs) and price_gap_state == "attractive":
>         out.append("B")
>     return out          # 可同时含 A、B；可为空（则不出买入结论）
> ```
> **关键约束（规则 9 / C123-4）**：`opportunity_types` **不进买入门**——两类都不得被额外门槛拒绝；分类是"**标注**"，不是"**准入**"。

### C.2 【必要区分修复】护城河"水平"字段（N2.2-02）

**问题**：仅有 `moat_state`（趋势），缺"水平"字段。
**修复（落 Ch4）**：

| 字段 | 取值 | 语义 |
|---|---|---|
| `moat_level`（**新增**） | `weak / moderate / strong / tbd` | 护城河**水平**（整体强弱） |
| `moat_state`（既有） | `strengthening / weakening / unchanged / tbd` | 护城河**趋势**（变化方向） |

> 两字段**独立**、可同时出现且不同向（对齐 N2.2-04 三维不同向）。断言：二者不得合并为单一 `moat` 字段。

> **待定 token 规范（v1.1 回改 F-10 / R-14）**：包内所有"待定 / 未确定"**枚举值**统一为小写 **`tbd`**；历史拼写 **`tdb`**（字母颠倒）与长式 **`to_be_determined`** **已归一，不得再用**。适用字段：`moat_level`、`moat_state`、`growth_momentum_state`、`certainty_state` 等一切"待定"取值。**中文散文里的"待定"叙述不受此约束**（仅约束枚举 token）。

> **命名规范（v1.1 回改 R-15~R-21，与"待定 token 规范"并列）**：
> ① **枚举取值一律小写 + 下划线**（`snake_case`），**禁止中文取值**（中文 token 做 `==` 等值判断，一旦同义词写歪即静默不等，review 也看不出）。历史中文取值已归一：`direct_knowledge → {direct, indirect, unknown}`、`price_gap_state → {fully_reflected, not_fully_reflected, attractive}`。
> ② **同名不同域必须消歧**：来源分级保留 `tier`，数据源 allowlist 改用 **`license_tier`**；`driver.realization_stage` 与 `relations.relation_progress_stage` **分别命名、不得互用**。
> ③ **一个概念只允许一个字段名**：禁止 `obtainable` / `obtainable_content_type` 并存（已统一为 `obtainable_content_type`）。
> ④ **留痕规范（R-23）**：**文件头版本行只写「概括 + 指针」，不再逐条枚举 R 编号**（枚举每回改一次即漂移、会触发反复追改；指针不会）；所有改动明细以各文件文末「回改记录」为**唯一权威**。本规范自 R-23 起适用全包。

> **`claim` 三字段规范（R-17）**：`claim_nature`（认识论性质 `fact/plan/forecast/interpretation`，普遍适用）/ `claim_form`（来源形成形式 `original_investigation/citation/estimate/opinion`，1.5 手拆型）/ `official_claim_kind`（官方一手披露性质 `occurred_fact/forward_guidance/marketing_claim`，**仅 `tier=primary`**）三者**维度不同，不得互换、不得合并**。

### C.3 其余已就绪的"必要区分"（无需改动，仅断言）

| 区分 | 字段 | 状态 |
|---|---|---|
| 跑不赢 vs 无法判断 | `confident_underperform` / `uncertain` / `boundary_unresolved` | ✅ Ch7 已实现，补断言"boundary 不并入任一端" |
| 机会扩大 vs 兑现确定 | `growth_momentum_state` / `certainty_state` | ✅ Ch4 H.1 已实现 |
| 卖出≠持仓≠做空 | `action` 枚举无做空；无 position | ✅ 已实现（P-04/P-05 断言） |

---

## D. 决策门与前置校验

### D.1 【硬冲突 A-06 修复】提前判断三要素 = 买入前置必填（规则 6 / N2.1-06）

**问题**：`falsifiers`/`unmet_conditions` 字段虽存在，但**未列为买入前置必填**——"提前判断"（未兑现前买入）建议缺少"假设/证据缺口/验证条件"仍能出建议。
**修复**：在 Ch7 `gate.py` 增加**强制前置校验**，三要素**任一为空即不得出建议**（记 gap 而非出建议）。

| 必填项 | 字段（与第一章 §C.1 **同名同拼写**） | 与 Ch7 8 字段衔接 |
|---|---|---|
| 假设 | `assumptions[]` | 第 5 字段"价值与价格" + `ExplainableForecast.drivers` |
| 证据缺口 | `evidence_gaps[]` | 第 8 字段 `unmet_conditions` / `data_gaps` |
| 验证条件 | `verification_conditions[]` | 第 8 字段 `falsifiers` / `expiry_review` |

```python
# scripts/decision/gate.py（新增校验，插入买入门）
def assert_early_judgment_complete(rec) -> None:
    trio = {
        "assumptions": rec.assumptions,
        "evidence_gaps": rec.evidence_gaps,
        "verification_conditions": rec.verification_conditions,
    }
    for name, val in trio.items():
        if not val:                       # 空 → 拦截
            raise EarlyJudgmentIncomplete(
                f"提前判断缺必填项: {name}",
                action="记 gap（不出建议）")
    # 与第一章 G1-01 一致：三要素须与结论同版本写入
    assert rec.trio_written_seq <= rec.version_seq
```

> **边界**：**非提前判断**（已兑现）建议**不**强制三要素；判断"是否提前判断"由"结论是否依赖未兑现假设"决定（Ch4 H.1 + Ch5 explainable forecast）。

### D.2 horizon 区间校验（规则 1 / N2.1-01，需补充）

```python
def assert_horizon(rec) -> None:
    assert rec.start_date is not None
    assert QUARTER <= rec.horizon <= YEAR, "horizon 越界 [1Q,1Y]"
```
- 边界值（正好 1Q / 1Y）**包含**在内；超范围 → 拒绝。
- 校验器挂 `recommendations` schema 断言（L1）。

### D.3 反拖延检测（N2.2-05 / C123-6，需补充）

可测规则（阈值 `N` 待冻结，建议 2，落 `rules/review.yaml`）：

```python
def detect_horizon_extension(chain: list[Rec], N: int) -> list[Flag]:
    flags = []
    ext = 0
    for prev, cur in pairwise(chain):
        if cur.horizon > prev.horizon:               # 期限被延长
            ext += 1
            if ext >= N:                             # ① 连续延长 ≥ N 次
                flags.append("horizon_extension_suspicious")
            if days_before(cur.written_at, cur.review_date) <= 3:  # ② 临近复查日
                flags.append("horizon_extension_suspicious")
            if cur.assumption_changed is False:      # ③ 未更新假设
                flags.append("horizon_extension_suspicious")
        else:
            ext = 0                                   # 有缩短则重置
    # ④ 触发即转人工复核 + 记 vision_drift，不得静默延期
    return flags
```
- 命中 → 转**人工复核** + 记 `vision_drift`；**禁止静默延期**。

### D.4 强制复查（规则 11）与"排序≠门槛"（C123-4/5）

- 价格下跌**只触发强制复查**（Ch7 R5），**无止损**（P-03）、**不自动判低估**（承 Ch5 F.5）。
- 决策门**只做结构判定**（有假设+期限+收益+DerivedValue 底稿），**无任何置信度数值比较**（对齐 N2.3-02 / P-02）。
- 建议排序用 Ch6 `priority_order` 字典序（处理顺序），**不拒绝任何建议**。

---

## E. 规则版本、变更审计与展示层中立

| 项 | 位置 | 要点 |
|---|---|---|
| 规则只读 | `rules/**`（0444 + SHA256） | 模型不可写（承 N9.2-01 / N9.1-25）；注入防御 |
| 规则版本绑定 | `recommendations.rule_version` | 每条建议记录当时生效规则版本 |
| 变更审计 | `audit/rule_changes.jsonl` | 任何影响买卖条件的改动（**含展示逻辑**）留痕 |
| 展示层中立 | `scripts/views/neutrality_check.py`（L5） | 排序/颜色/折叠不得形成买卖倾向；反证与缺口同等可见（P-10） |
| 数据/指令分离 | 继承第九章 | 抓取文本不得被当作指令执行 |

---

## F. 回改清单（前五章逐文件逐节）+ 与其它文件的接口

> **本清单是 A-02 / A-06 修复的落地指引**。第一章、第三章方案中的同名结论**均指向本清单**，不重复。

| # | 目标文件 | 节 | 现状 | 改成什么 | 对应需求 | 关联冲突 | 执行状态（v1.1） |
|---|---|---|---|---|---|---|---|
| R-01 | `07_产业链传导与股票建议/02_实现方案.md` | §E.1 `recommendations` 字段表 | 无机会类型字段 | 增 `opportunity_types[]`（enum A/B）+ `opportunity_basis[]`（每类一条依据） | N2.1-02 | **A-02** | ✅ 已执行 · 07 §A.4/§E.3 |
| R-02 | `07_.../02_实现方案.md` | §D.1 gate / §D.4 | 门未涉及机会类型 | 明确机会类型**不进买入门**；门后由 `classify_opportunity()` **标注**（两类均可买入，规则9不得额外拒绝） | N2.1-02/09 | A-02 | ✅ 已执行 · 07 §D.6 |
| R-03 | `07_.../02_实现方案.md` | §D.2 / §D.4 / §E.1 | `falsifiers`/`unmet_conditions` 存在但非 gate 必填 | 增 `assert_early_judgment_complete()`：`assumptions[]`/`evidence_gaps[]`/`verification_conditions[]` 三字段**非空方可通过提前判断门**（空则记 gap，不出建议） | N2.1-06 | **A-06** | ✅ 已执行 · 07 §D.7 |
| R-04 | `07_.../02_实现方案.md` | §E.1 schema | 缺 `horizon` 区间约束 | 增 `horizon ∈ [1Q,1Y]` 断言 + 越界拒绝；`start_date` 必填 | N2.1-01 | 规则1 | ✅ 已执行 · 07 §E.3 |
| R-05 | `04_公司价值研究与深度标准/02_实现方案.md` | §H.1 价值状态输出 | 缺"水平"字段 / 未暴露机会分类输入 | ① 增 `moat_level`（水平），与 `moat_state`（趋势）**独立**；② 输出 `value_state_refs`（A/B 类依据来源）供 `classify_opportunity` 消费 | N2.2-02 / N2.1-02 | A-02 | ✅ 已执行 · 04 §H.1/§H.4 |
| R-06 | `05_价格与市场预期研究/02_实现方案.md` | §E / §F 价格缺口 | 独立判断 vs 价格隐含为内部计算 | 显式暴露 `price_gap_state`（fully_reflected / not_fully_reflected / attractive）供机会分类消费 | N2.1-02 | A-02 | ✅ 已执行 · 05 §E.6 |
| R-07 | `09_数据与实现约束/09_需求拆解与实现方案_v1.md` | §1.1 / §3.3.4 `recommendations` 固定字段 | 未含机会类型 / 提前判断三要素 | 固定字段表增 `opportunity_types`、`assumptions`、`evidence_gaps`、`verification_conditions`（均 version-bound）；增 `horizon ∈ [1Q,1Y]` 约束 | N2.1-02/06/01 | **A-02 / A-06** | ✅ 已执行 · 09 §1.1/§3.3.4 |
| R-08 | `09_..._v1.md` | §2.1.3 schema | 未显式允许公司 0 证券 | 显式声明 `company → 0..N securities`（未上市实体 OpenAI/Anthropic 建基线、**不出建议**）+ 测试用例 | N3.2-08 | Ch3 B-02 | ✅ 已执行 · 09 §2.1.3 |
| R-09 | `04/05/07` 实现方案 | 环境层承载节 | 五类环境项散落 | 明确承载：利率→估值假设、融资→驱动代价、AI需求→`demand_signal`、能源→传导门槛、政策→event/impact | N3.3-01 | Ch3 B-07 | ✅ 已执行 · 04 §D.4 / 05 §D.5 / 07 §A.6 |
| R-10 | `09_..._v1.md` + `05_.../02_实现方案.md` | 基准收益计算 | 无"禁用替代指数/二次扣费"守卫 | 引用本章新增 `scripts/benchmark/return_guard.py`：① 禁止用公开股票指数替代基金；② 相对收益禁止二次扣费，净值标"诊断" | N3.4-05/07 | Ch3 B-11/B-13 | ✅ 已执行 · 09 §3.4.12 / 05 §E.6 |
| R-11 | `09_..._v1.md` | §3.4 变更 | 有版本+原因，无人工审批 | 基准变更增**人工审批闸门**（承第十一章"发布与人工复核"） | N3.4-09 | Ch3 B-15 | ✅ 已执行 · 09 §3.4.12 |
| R-12 | `09_..._v1.md` | §2.1.3 收录 | 未禁目标数量配置 / 未强制纳入理由 | ① 增 denylist（无 `target_company_count`/`expected_list_size`）；② 正式名单入选须"原始出处 + 纳入理由 + 证据强度"三字段非空 | N3.1-01/08/09/10 | Ch3 B-18 | ✅ 已执行 · 09 §2.1.3 |
| R-13 | `06_公开信息与证据筛选/02_实现方案.md` + `07_...` | 展示层 | 无渲染中性守卫 | 引用 `scripts/views/neutrality_check.py`（P-10 + N3.2-05） | N2.3-01 / N3.2-05 | Ch3 B-06 | ✅ 已执行 · 06 §H.2 / 07 §A.5 |
| R-14 | `04/02` · `02/01` · `02/02`（枚举定义处） | `04 §H.1/§H.4` · `02/01 §2` · `02/02 §C.2` | "待定"枚举三写法并存（`tdb` / `to_be_determined` / `tbd`） | 归一为小写 **`tbd`**：`moat_state` / `growth_momentum_state` / `certainty_state` 的 `tdb`·`to_be_determined` 全部改 `tbd`（`moat_level` 已合规） | N2.2-02 | 术语一致性（F-10） | ✅ 已执行 · 02 §C.2 规范声明 / 04 §H.1·§H.4 / 02/01 §2 |
| R-15 | `06/01_需求拆解.md` | §2 证据质量表 | `direct_knowledge` 用**中文字面取值**（`{直接, 间接, 未知}`） | 统一**英文** `{direct, indirect, unknown}` | N9.2-02 | C-1（F-11） | ✅ 已执行 · 06/01 §2 |
| R-16 | `02/02` · `05/02` · `07/02` | `02 §C.1` · `05 §E.6` · `07 §E.3` | `price_gap_state` 用**中文字面取值**且参与 `==` 等值判断 | 统一**英文** `{fully_reflected, not_fully_reflected, attractive}`，同步等值判断 | N2.1-02 | C-1（F-11） | ✅ 已执行 · 02 §C.1 / 05 §E.6 / 07 §E.3 |
| R-17 | `06/01` · `06/02` · `09` · `09_需求覆盖矩阵_v1.md` · `04/01` · `04/02`（后两者**仅复核、无 R-17 文本改动**，实际改动并入 **R-22**） | `§1.2` · `§2.3` · `§A.1` · `§D.1` · `§F.2` · `§I.2` · `附表T03` · `§3.4.6` · `§N9.1-13` | C-2/C-2b：`claim_nature` / `claim_type` **概念串位** | 拆为三字段：`claim_type` → **`claim_form`**（`original_investigation`/`citation`/`estimate`/`opinion`）；"官方一手三分"的 `claim_nature` → **`official_claim_kind`**（`occurred_fact`/`forward_guidance`/`marketing_claim`，仅 `tier=primary`）；"事实/计划/预测/解释"的 `claim_nature` **保持不变** | — | C-2/C-2b | ✅ 已执行 · 06 §N6.2-03/N6.2-04 · 09 §claims · 09矩阵 §N9.1-13 |
| R-18 | `01/02` · `02/02` · `02/01` · `03/01` · `03/02` · `06/02` · `09` | D-01 各落位 | allowlist `tier=free_quota` 与来源分级 `tier` **撞名** | allowlist 侧改名 **`license_tier=free_quota`**（`tier` 保留给来源分级） | N2.1-14 / N3.3-04 | C-3（F-11） | ✅ 已执行 · 全 7 处 |
| R-19 | `07/01` · `07/02` · `04/02` | `07 §B.1/§B.4` · `07/01` · `04 §H.1` | `relations.realization_stage` 与 `driver.realization_stage` **同名不同域** | relations 侧改名 **`relation_progress_stage`**；driver 侧保留并加注"仅用于 driver" | N7.1-10 / N4.2-01 | C-4（F-11） | ✅ 已执行 · 07 §B.1/§B.4 / 04 §H.1 加注 |
| R-20 | `06/01` · `06/02` | 来源登记 / 缺失处理 | `source.obtainable` 与 `obtainable_content_type` **名漂移** + `no_transcript` 取值越界 | 统一 **`obtainable_content_type`**，取值扩为 `{full_text, summary, snippet, headline_only, no_transcript}` | N6.1-07 / N9.3-02 | C-5（F-11） | ✅ 已执行 · 06/01 §… / 06/02 各引用 |
| R-21 | `04/02` | §H.1 驱动 yaml | `source_class` 列 8 token 但注"七类" | 改注 **"7 类 + `mixed` 兜底"** | N4.1-03 | C-6（F-11） | ✅ 已执行 · 04 §H.1 |
| R-22 | `04/01` · `04/02` · `06/01` · `09` | `§E.2/§E.3` · `§2.3(a)` · `§文件头` | ① `claim.nature` 点号漂移（点号后 `nature` 非真实字段名）；② 06/01 §2.3(a) 输出列未列全三字段；③ R-17 行目标文件/节号仍为占位；④ 09主 文件头未同步 R-17 | ① `claim.nature` → **`claim_nature`**（04/01 §2.2(b)、04/02 §E.2）；② 输出列补 `claim_form`/`official_claim_kind`；③ 补 R-17 行目标文件与节号；④ 文件头补 R-17/R-22 | N4.2-01 / N6.3-02 | C-2/C-2b（R-17 收尾） | ✅ 已执行 · 04 §E.2 / 06 §2.3(a) / 09 §文件头 |
| R-23 | `01/02` · `02/01` · `03/01` · `03/02` · `04/01` · `04/02` · `05/02` · `06/01` · `06/02` · `07/01` · `07/02` · `09` · `09矩阵`（13 处非 owner 文件；本文件 `02/02` 自身亦同步，见下行） | `§文件头`版本行 | 版本行内**逐条枚举 R 编号**（每回改一次即漂移，触发反复追改） | 归一为「**概括 + 指针**」：**删除 R 编号枚举**，仅保留"（2026-09-15 回改）"及该行原有其它信息（日期/待定项状态/统计等），行末追加 `；**本文件回改明细见文末「附：回改记录」（唯一权威）**`；回改明细以各文件文末「回改记录」为**唯一权威**；owner 文件（本文件）版本行写 `R-01~R-23 全部执行，见 §F` | 留痕规范 | 留痕规范（F-10 族） | ✅ 已执行 · 全 13 处版本行 |
| R-24 | `02_已确认的投资规则/02_实现方案.md` | §B.1 / §B.2 / §B.3 | P-04 裸词 `position` 无作用域声明，与合法同名词 `position_listed`（"产业链位置"收录态，见 03 §需求拆解 N3.2-04）/ `business_positions`（多业务位置数据对象，见 09 §2.1.3）碰撞，朴素全仓扫描必误报 | 补 L1 作用域声明（§B.1）+ P-04 行消歧（§B.2）+ Checker-2 作用域注释（§B.3）+ 新增「★ 禁词作用域通则」 | N2.1-03 / N2.2-06 | P-09 同族（scope 消歧） | ✅ 已执行 · 本文件 §B.1/§B.2/§B.3 |
| R-25 | `01/02` · `03/02` · `04/02` · `05/02` · `06/02` · `07/02` · `08/02` · `09_需求覆盖矩阵_v1.md` · `10/02` · `11/02` · 本文件 `02/02`（共 11 份） | 各文件「覆盖状态」图例行 / ⚠️ 状态列 / 统计叙述 / ⚠️ 处置表 | ⚠️ 定性写成「待需求方拍板 / 需需求方裁定 / 待裁定 / 主动上呈拍板 / 须由需求方拍板」，把"**值待冻结**"误述为"**未被覆盖 / 阻塞交付**" | 统一归正为 ⚠️ **已收口 · 待收尾**（机制已就绪、可直接运行、**不阻塞交付**；待收尾仅三类：① 值可配置待冻结 ② 依赖人工评审 / 技术侧补设计 ③ 依赖外部动作如跨章回改）；各章图例同文；并指向 `00_待拍板项清单` **D-03**（基线值已确认）/ **D-04**（口径归正）。**✅/⚠️/❌ 计数一律不变**（⚠️ 表达"值尚未冻结"这一事实，不因定性修正而变） | 跨章口径一致性 | D-03 / D-04 | ✅ 已执行 · 11 处覆盖矩阵 + 各文件版本行/回改记录；另：三份 `01/02`·`02/02`·`03/02` 的「技术待定项」表 ②–⑤ 状态列 `需需求方澄清` → `✅ 已确认（D-03）`，表头说明与结论行同步 |
| R-26 | `02_已确认的投资规则/02_实现方案.md` | §B.4 正文表 + mermaid `SRC` 子图 · §B.1 L3 行 | 配置扫描（L3 / Checker-4）范围仅 `rules/**/*.{yaml,json}`，第八 / 十 / 十一章**新引入配置未纳入**，"不得把待定项补成新门槛"在新层无守卫 | 显式纳入 Checker-4 配置扫描：`rules/trigger.yaml`（Ch8）· `rules/notification.yaml`（Ch8）· **三入口展示配置**（Ch8）· `rules/freeze.yaml`（**含 `freeze_param[p10].caliber_terms`**，Ch10/11）· `registry/delivery.yaml`（Ch11）；写明禁止项示例（`min_excess_return`/`stop_loss`/`confidence_threshold` 等，防"借参数补门槛"）；§B.4 正文表下方加「被检目标清单（含新层配置）」说明 + mermaid `SRC` 子图增节点（两处一致）+ §B.1 L3 行加指针 | N8.3-05 / N8.1-09 / N10.3-08 / N11.2-12 | S-01 / S-02 / S-03 / S-15 | ✅ 已执行 · 本文件 §B.4 / §B.1 L3 |
| R-27 | `02_已确认的投资规则/02_实现方案.md` | §B.2 P-09 行 · §B.3 Checker-1 · §B.1 L2 行 | `is_decision_scope` 判定口径仅写"位于 decision/gate 模块或 classify_*/assert_* 调用链"，未说明通知 / 排序、复盘标记字段、`freeze_param` 的归属 | 三类调整（并存、方向不同）：① **纳入**通知 / 每日页排序函数（排序统一走 `priority_order` 字典序，禁 `weight`/`score`）② **排除**复盘标记字段 `eval_layer`/`error_axis`/`error_axis_note`（不进 decision/graph 作用域）③ **排除** `freeze_param` 读取（配置 / 标记，非决策输入）；§B.2 P-09 行加注 + §B.3 Checker-1 注释补充 + §B.1 L2 行同步 | N8.1-07 / N8.1-09 / N10.3-12 / N11.2-12 | P-09 同族（scope 消歧） | ✅ 已执行 · 本文件 §B.2 / §B.3 / §B.1 L2 |
| R-28 | `02_已确认的投资规则/02_实现方案.md` | §B.1「★ 禁词作用域通则」第 4 点 · §B.3 Checker-1 | 通则第 4 点仅列 **2 个实例**（P-09、P-04），未覆盖"检查器自身命名含禁词"导致的朴素扫描误报；且仅声明 scope、Checker-1 内无可执行豁免，`scan_ast("scripts")` 会扫到 `scripts/checks/**` 自身而卡死 CI | 新增**第 3 个实例**「**检查器命名空间免扫**」：检查器自身函数名 / 变量名可能含禁词（如 `assert_no_composite_score`、`check_no_weight`），朴素扫描会误报；scope = 仅在 `scripts/checks/**` 与 `tests/**` 的检查器命名空间内免扫；并将"两次实例"改为"**三次实例**"。**只做消歧，不新增禁止项、不放松约束**。**含 Checker-1 可执行豁免**：§B.3 `scan_ast` 循环内加 `is_checker_namespace(path) → continue`（`ast.parse` 之前）+ 注释块补第 4 条「免扫」，使 scope 满足通则第 3 点"必须可执行" | 跨章口径一致性 | P-04 / P-09 同族（scope 消歧） | ✅ 已执行 · 本文件 §B.1 / §B.3 Checker-1 |
| R-29 | `09_数据与实现约束/09_需求拆解与实现方案_v1.md` | 新增 §3.3.5（紧接 §3.3.4 之后） | 无 `eval_result` 字段定义 | 新增 `eval_result` 固定字段表：`eval_layer`（enum 三层）/ `error_axis`（enum[] 七方向）/ `error_axis_note`（string），标注"标记字段、不进决策函数" | N10.3-01/12 | S-04 | ✅ 已执行 · 09 §3.3.5（含 token 与 Ch10 权威对齐） |
| R-30 | `02_已确认的投资规则/01_需求拆解.md` · `01_产品目标与核心闭环/01_需求拆解.md` · `08_产品入口与每日运行/01_需求拆解.md` · `03_覆盖范围与基准/01_需求拆解.md`（各 §5 T 映射表） | 各 §5 | 5 处跨章 T 引用与源稿不一致（T12 两处 / T13 一处 / T10 一处 / T03 一处）+ T09 精度 | 逐条回改（权威出处 10/01 §2.2(c)；裁决见 10/01 §3.1 C10-3：以源稿为权威） | N10.2-02 | C10-3 / 🟡×4 | ✅ 已执行 · `02/01` · `01/01` · `08/01` · `03/01` 各 §5 T 映射表（+ 各文件回改记录） |
| R-31 | `08_产品入口与每日运行/02_实现方案.md` · `10_验收与持续复盘/02_实现方案.md` · `11_交付顺序与实施参数/02_实现方案.md`（各「建议回改项」节） | 各「建议回改项」节标题 + 说明句 + 段末「说明」句 | 三节标题仍为「（待主理人统一登记）」，说明句仍称"本文件不自行修改任何既有文件，仅登记如下"、段末「说明」仍称"建议主理人…统一登记（新编号续接）"——其所列项实已于收口轮执行，收口状态未归正 | 节标题「（待主理人统一登记）」→「（**已收口 · 登记于 `02/02 §F` R-26~R-30**）」；说明句归正为"所列项已于收口轮按三段式审计链回改执行，编号见 `02/02 §F`（**唯一权威登记**）"，保留原「格式：」行；**含各节段末「说明」句归正**：改写为完成时"已由主理人在收口轮统一登记为 R-26~R-30 并执行完毕（见 `02/02 §F`，唯一权威）；本表「现状 / 改成什么」两列保留原始登记内容，供追溯"（`08/02`→R-26/R-27；`10/02`→第 1 条 R-29、第 2–3 条 R-27/R-26、第 4–5 条 R-30；`11/02`→R-26/R-27），**原表一行不删**；除上述三处外**不改动三份文件其它任何内容** | 跨章口径一致性 | S-01 / S-02 / S-03 / S-04 / S-07 / S-15 | ✅ 已执行 · 三份 §「建议回改项」（节标题 + 说明句 + 段末说明） |
| R-32 | `01_产品目标与核心闭环/01_需求拆解.md` · `01_产品目标与核心闭环/02_实现方案.md` | `01/01 §2.2 / §4 / §6` · `01/02 §D.1 / §H` + 两文件版本行/回改记录 | 10+1 处过时表述（写于第八/十章完成前）：「第八章/第十章未拆解」「❌ 缺口」「尚无章节承接」「拆解后即插即用」「7/8 步属待拆章节」等，与 `01/02` 自身章头（第 7/8 步已由第八/十章承载）**直接矛盾** | 承接状态与交接要点归正为"已拆解"：`01/01` §2.2 承接表第 7/8 步 ❌缺口 → ✅已拆解（`08/02` / `10/02`）+ 表下加注 + 「其它跨章缺口」两条改"已承接" + §4 阶段表 + §6 交接要点第 6 条；`01/02` §D.1 8 步状态表同改 + 编排器职责"拆解后即插即用"→"已拆解、即插即用" + §H 接口"（未拆解）"→"（已拆解 · `08/02` / `10/02`）" | N1.2-01 | 交付可读性（过时表述） | ✅ 已执行 · `01/01` §2.2/§4/§6 · `01/02` §D.1/§H |
| R-33 | `02/02` §F · `01/02` 文末回改记录 · `08/02` · `10/02` · `11/02`（各文件头版本行 + 文末回改记录） | `02/02 §F`（R-32 行节号）· `01/02` 回改记录 · 三份 `02` 之「建议回改项」+ 文件头 + 文末回改记录 · `10/02` §「本章不做什么」/ §覆盖矩阵 N10.2-02 / §统计叙述 | ① **R-32 登记节号笔误**：8 步状态表实际在 `01/02 §D.1`（`§B` 是「WorkBuddy 复用边界」），`02/02 §F` R-32 行与 `01/02` 回改记录均误写 `§B`；② **R-31 三段式审计链断链**：`02/02 §F` ① 已登记，但 `08/02`·`10/02`·`11/02` 的 ② 文末回改记录、③ 文件头版本行均未加 R-31 指针；③ **`10/02` 三处过时表述**：「本章不做什么」第 4 条"不自行修改既有文件…跨章问题只在文末「建议回改项」登记"、§覆盖矩阵 N10.2-02 状态格"5 处跨章引用**待回改**"、§统计叙述"N10.2-02…**待主理人统一回改**" | ① 节号订正为 `01/02 §D.1 / §H`（共 5 处：`02/02` §F 3 处 + `02/02` 文末 1 处 + `01/02` 回改记录 1 处）；② 三份文件各补文末回改记录 **R-31 行** + 补文件头版本行留痕指针，补齐 ②③（**版本行格式随后由 R-34 统一归一**）；③ `10/02` 三处归正为完成时（"已于收口轮按 R-30 回改执行完毕，见 `02/02 §F`"）；**✅/⚠️/❌ 计数一律不变、⚠️ 定性不变**；④ 全程只改列明位置，不改动其它内容 | 留痕规范 / N10.2-02 | F-10 族（留痕）/ 交付可读性（过时表述） | ✅ 已执行 · `02/02 §F` / `01/02` / `08/02` / `10/02` / `11/02` |
| R-34 | 15 份非 owner 文件（`01/01` · `01/02` · `02/01` · `03/01` · `03/02` · `04/02` · `05/02` · `06/02` · `07/02` · `08/01` · `08/02` · `09` · `09矩阵` · `10/02` · `11/02`） | 各文件头「版本」行 | 版本行内含 **R 编号子句**（`；R-25 覆盖矩阵口径归正（…）` / `；R-30 §5 T 映射回改（…）` / `；R-31…；R-33…` / `R-29 新增 §3.3.5 …`）——即 R-23 明禁的「逐条枚举 R 编号」。**实证漂移**：R-31 改了 `08/02`·`10/02`·`11/02` 却未加版本行子句，致其版本行仅标 R-25（本轮 R-33 才补回）；且每轮需回改全部受影响文件版本行，逐轮线性膨胀 | 删除全部 R 编号子句，归一为 R-23 规范形态：`v1.1（2026-09-15 回改[＋该行原有其它信息]）。本文件回改明细见文末「附：回改记录」（**唯一权威**）。`；**行内原有其它信息（待定项状态 / 需求统计 / 覆盖矩阵升级说明 / 含 D-01 等）一律保留**；owner 文件 `02/02` 按 R-23 豁免（保留「`R-01~R-XX 全部执行，见 §F`」形态，区间随轮次推进，本轮已推进至 **R-34**）。**本次为全包统一格式归一（同 R-23 先例），不逐文件加 ② 行；② 以本行「全 15 处版本行」为准** | 留痕规范 | R-23 回归（F-10 族） | ✅ 已执行 · 全 15 处版本行 |
| R-35 | 新建根级 `00_交付施工图.md` · `00_交付索引.md`（本包仓库根） | ——（新增件，无被改节） | 仓库根有 11 章产出 + 覆盖矩阵 + `00_待拍板项清单.md`，**无任何索引**；唯一「阅读说明」位于**嵌套的上游源包目录**内（讲源包自身结构，**不覆盖本包 11 章产出**）；且缺"按什么顺序施工"的收敛视图 | ① 新建 **`00_交付索引.md`**：11 章成果导航 + 全局件（待拍板清单 / 施工图）+ 按目的阅读分流 + 命名与留痕规范速查 + 上游源件区分；② 新建 **`00_交付施工图.md`**：以 **Ch11 五阶段为施工主线**（`09 §3.9` 为实现细化挂靠），把 11 章收敛为"**交付物 / 依赖 / 验收点 / WorkBuddy 口径 A·B·C / 工作量**"，附全局组件总表、目录地基、关键路径与并行机会，并登记施工前待核 **N-01~N-05**（含 `10/02 §I.2` 的 T 权威表指向不一致）。两新文件各自含版本行 + 文末回改记录（②③ 齐） | 交付实施准备（收口后新增件；2026-09-15 需求方拍板"新增根级索引"） | N-01（T 权威表指向） | ✅ 已执行 · 新建 2 文件 |
| R-36 | 新建根级 `00_开发Agent开工提示词.md`（本包仓库根） | ——（新增件，无被改节） | 11 章实现方案是**设计真相源**，但**面向"人读"**（逐章、含大量口径注与留痕表），**不能直接喂给实现方 Agent**：缺"开工顺序 / 完成定义 / 禁止清单 / 证据要求"，实测易产出占位符、未接线模块、偏离设计的 demo。且设计中的 `tbd` 占位与"代码占位符"是**两个东西**，须显式划界，否则实现方或全禁或全放 | 新建 **`00_开发Agent开工提示词.md`**（**自包含**，接收方无需本对话上下文）：身份与三条底线（真实现 / 真接线 / 真验证）+ **禁写代码先建认知**（阅读顺序 + 设计即权威）+ 工程根 `/Users/gaza/Developer/InvestSigh` 与 18 JSONL 地基 + 五阶段门禁（含阶段内并行组）+ **任务 SOP（DoD 先行 → 证据先行 → 卡住先停）** + **四种"假交付"判定**（`tbd` 唯一合法占位边界 / 接线失败四类 / 无效测试四形态 / 偏离设计 8 个高发点）+ 施工纪律 12 条 + `Phase N` 集成与硬化（含"扫描范围只写 `code_root`"的降噪铁律）+ 独立验收审计提示词 + 会话与上下文纪律 + 待核 N-01~N-05 + 交付前 10 问。含版本行 + 文末回改记录（②③ 齐） | 交付实施准备（收口后新增件；2026-09-15 需求方指令"拟一份提示词给将要开始开发的 Agent"） | N-01~N-05（承 `00_交付施工图.md §7`） | ✅ 已执行 · 新建 1 文件 |
| R-37 | `00_开发Agent开工提示词.md` · `00_交付施工图.md` · `09_数据与实现约束/09_需求拆解与实现方案_v1.md` · 本文件 §F R-36 行与文末 R-36 行 | 提示词 §一 / §三 / §五 / §八 · 施工图 §2 阶段① 骨架行 / §4 · `09 §3.3.3` 目录布局首行 · 本文件 §F R-36 行与文末 R-36 行 | **运行环境由 Windows 迁至 macOS**：全包尚有 13 处硬编码 Windows 路径（`D:\FastInvest\aichain\` 等），而工程根在 Mac 上为 `/Users/gaza/Developer/InvestSigh`——若不同步，实现方会读到**互相矛盾的工程根**（提示词说 Mac、施工图与 `Ch9 §3.3.3` 说 Windows） | ① **工程根统一**：`D:\FastInvest\aichain\` → **`/Users/gaza/Developer/InvestSigh`**（施工图 §2 骨架行 / §4 工程根与目录树 · `09 §3.3.3` 目录布局首行 · 提示词 §三 工程根与目录树 / §十一 第 3 步）；② **运行环境声明**：提示词 §三 首句加"运行环境 = macOS（路径以 `/` 分隔）"；③ **需求包位置改为可自证**：提示词 §一 原硬编码路径 → "需求对齐包根 = 本文件所在仓库根，用 `git rev-parse --show-toplevel` 取"；§三 两条 ⚠️ 同步（"不要动 `00_*` 与 `01_`~`11_`"；父目录警告改写为 `/Users/gaza/Developer/`）；④ **扫描范围示例改绝对路径**：提示词 §八 `N-2` 前一句 `code_root` 由"如 `aichain/`"→"= `/Users/gaza/Developer/InvestSigh`"；⑤ **脚本可移植性**：§八 `N-2` 与 §五 `AC-07` 不再依赖某台机器的 skill 路径——补"**本地没有该脚本就自己写一个**"及其检测规格与退出码约定；⑥ 本文件 §F 与文末 R-36 行内的示例路径同步更新 | 交付实施准备（环境迁移；2026-09-15 需求方："我在新的苹果电脑上让 Agent 进行开发，那边的工程根是 `/Users/gaza/Developer/InvestSigh`，进行相应修改"） | 无（纯环境路径同步，不涉策略与门槛） | ✅ 已执行 · 4 文件 13 处 |

**跨文件一致性声明（三份文件共用）**：
- 字段拼写统一为 `opportunity_types[]` / `opportunity_basis[]` / `assumptions[]` / `evidence_gaps[]` / `verification_conditions[]` / `moat_level` / `moat_state` / `horizon` / `start_date`。
- "提前判断三要素 = 买入前置必填"在 **第二章 §D.1**（规则侧）与 **第一章 §C.1**（同版本写入侧）**描述一致、互为补充**。
- "待定"**枚举值**统一为小写 **`tbd`**；历史 `tdb` / `to_be_determined` 不再使用（见 §C.2 规范声明）。

---

## 覆盖矩阵（第二章，23 条）

> 覆盖状态：✅ 已覆盖 ｜ ⚠️ **已收口 · 待收尾**（机制已就绪、可直接运行，**不阻塞交付**；待收尾项仅三类：① **值可配置待冻结** ② 依赖**人工评审 / 技术侧补设计** ③ 依赖**外部动作**（如跨章回改）。**均非技术不可行、非设计缺陷**）｜ ❌ 缺口。

| 需求ID | 优先级 | 实现载体 | 验收方式 | 所属交付阶段 | 覆盖状态 |
|---|---|---|---|---|---|
| N2.1-01 | P0 | `assert_horizon`（§D.2）+ schema 断言 | `horizon` 越界被拒，`start_date` 必填 | 阶段2 | ✅ |
| N2.1-02 | P0 | `opportunity_types[]`+`classify_opportunity`（§C.1） | 两类均可产出；字段存在 | 阶段2 | ✅ 修复A-02 |
| N2.1-03 | P0 | 建议映射 `security_id`；无仓位字段 | P-04 断言通过 | 阶段1 | ✅ |
| N2.1-04 | P0 | 买入不要求盈利 | P-06 断言（无 `profitability_required`） | 阶段3 | ✅ |
| N2.1-05 | P0 | moat 不入买入门 | P-06 断言（gate 输入无 moat） | 阶段3 | ✅ |
| N2.1-06 | P0 | 提前判断三要素前置必填（§D.1） | 三要素缺一 → 拦截/记 gap | 阶段2 | ✅ 修复A-06 |
| N2.1-07 | P0 | 官方确认非门控（`confidence_boost`） | 无 official gate；A4 已确认：**纯加成、永不参与比较** | 阶段3 | ✅（A4 已确认）|
| N2.1-08 | P0 | 买入判定恰为两条 | 规则唯一性检查 | 阶段3 | ✅ |
| N2.1-09 | P0 | 无最低超额门槛 | P-01 AST+配置扫描通过 | 阶段2 | ✅ |
| N2.1-10 | P0 | 卖出 = 相对跑输（`confident_underperform`） | 正收益跑不赢→卖出 | 阶段3 | ✅ |
| N2.1-11 | P0 | 强制复查、无止损 | P-03 断言 + 只触发复查 | 阶段4 | ✅ |
| N2.1-12 | P0 | 催化剂非硬门 | P-06（无 `catalyst_required`） | 阶段3 | ✅ |
| N2.1-13 | P0 | 每日 Automation + 事件触发 | 调度配置检查 | 阶段4 | ✅ |
| N2.1-14 | P0 | 首版仅公开信息（D-01：iFinD/pandadata = `free_quota`） | `data-sources.allowlist.yaml` 校验（含 `free_quota`） | 阶段1 | ✅ |
| N2.2-01 | P0 | `confident_underperform`/`uncertain`/`boundary_unresolved` | 三态可区分，boundary 不并入任一端 | 阶段3 | ✅ |
| N2.2-02 | P0 | `moat_level`+`moat_state`（§C.2） | 两字段独立可查 | 阶段3 | ✅ 修复 |
| N2.2-03 | P1 | `growth_momentum_state`+`certainty_state` | 两字段分离 | 阶段3 | ✅ |
| N2.2-04 | P0 | 三字段独立可不同向 | 禁止单一总状态（schema） | 阶段3 | ✅ |
| N2.2-05 | P0 | 反拖延检测（§D.3） | 连续延长 ≥N → 标记+人工复核 | 阶段4 | ⚠️ **已收口 · 值可配置待冻结**（N，基线值 B1 = 2）|
| N2.2-06 | P0 | 卖出≠持仓≠做空 | P-04/P-05 断言通过 | 阶段3 | ✅ |
| N2.3-01 | P0 | 规则版本+审计+展示中立（§E） | L5 展示检查 + 审计留痕 | 阶段5 | ✅ |
| N2.3-02 | P0 | 无固定置信度门槛 | P-02 AST 扫描通过；gate 无数值阈值；A5 已确认 | 阶段2 | ✅（A5 已确认）|
| N2.3-03 | P1 | 估值含上下行情景+不确定性 | 承 Ch5 场景一致性 | 阶段3 | ✅ |

**统计**：✅ 22 / ⚠️ 1 / 缺口 0。（**2 条硬冲突 A-02、A-06 已在本章闭环修复**；⚠️ = **已收口 · 值可配置待冻结**（N 的反拖延阈值），**非技术不可行、非设计缺陷、不阻塞交付**。）

> 各待收尾项的**基线值**（2026-09-15 已确认）与口径归正说明见 `00_待拍板项清单.md` **D-03 / D-04**；**⚠️ 计数不因口径归正而改变**（⚠️ 表达的是"值尚未冻结"这一事实，不是"未被覆盖"）。

---

## 技术待定项（第二章）

> 本表 ②–⑤ 已由需求方**确认**（见 `00_待拍板项清单` **D-03**，2026-09-15），作为**基线口径**；实现方保持可切换，**不得把待定项补成新门槛**。

| # | 待定项 | 影响 | 当前处理 | 状态 |
|---|---|---|---|---|
| ① | ~~规则 14：iFinD / pandadata MCP 连接器是否属"付费数据订阅"~~（同 Ch3 B-19） | 决定是否可从"首版可用清单"调用 | **D-01（2026-09-15 已拍板）**：**有免费额度，暂不视为"付费数据订阅"**；标 `license_tier=free_quota` / `cost=0`；**不得写入裁决逻辑**（仍走 Ch6 分级与证据质量流程）；备注"若未来转为付费，须按 N2.1-14 重新评估" | ✅ **已澄清（2026-09-15）** |
| ② | **规则 7：官方确认 `confidence_boost` 是否可能演变为门控/阈值** | 决定其是否为纯加成 | 仅作非门控加成，不参与任何比较 | ✅ **已确认（D-03 · 2026-09-15）** |
| ③ | **N2.3-02：决策门是否含"置信度数值阈值"**（C123-5） | 决定 gate 是否合规 | gate 仅做结构完备性校验，无数值阈值 | ✅ **已确认（D-03 · 2026-09-15）** |
| ④ | **Ch3 B-17：非美证券是否进入首版推荐范围** | 影响首批可推荐标的集合 | 研究覆盖 ⊇ 推荐范围；`recommended_security_scope` 参数化（默认美股） | ✅ **已确认（D-03 · 2026-09-15）** |
| ⑤ | **主基准 ETF 最终冻结（是否 AGIX）+ 收益口径（信号生效价/交易成本/税前口径）** | 决定相对收益基准与口径 | 基准以对象承载、状态"未冻结"；口径版本化 | ✅ **已确认（D-03 · 2026-09-15）** |

> **本条原"开放 4 条（② ③ ④ ⑤）"已于 2026-09-15 由需求方确认**（`00_待拍板项清单` **D-03**），作为**基线口径**（经 `get_param` 统一读口、机制上仍可切换）；第 ① 条此前已关闭。**原判定过程保留、不删除。**

> 另：反拖延阈值 `N`（建议 2）、价格下跌强制复查阈值 —— 属**实施参数**，走第十一章冻结，**非需求方策略问题**，故不计入上述开放 4 条。

> 说明：上述（开放 4 条：②–⑤）为**跨三章共用的同一组待定项**，在 `01/02/03` 三份文件的"技术待定项"中**逐份集中列出，措辞一致**。

---

## 附：回改记录（v1.1 · 2026-09-15）

| R 编号 | 本节改动 | 对应需求 | 关联冲突 |
|---|---|---|---|
| R-01~R-13 | §F 表新增「执行状态（v1.1）」列，逐条标 `✅ 已执行` + 落位节号（前五章已按 R-01~R-13 完成回改） | N2.* / N3.* | A-02 / A-06 / Ch3 B-* |
| D-01 | 文末技术待定项 ① 关闭：iFinD/pandadata 有免费额度、暂不视为付费数据订阅（标 `free_quota`，不进裁决逻辑）；保留原判定过程 + 追加结论行 | N2.1-14 / N6.1-13 | A-14 / B-19 |
| 矩阵校正 | 覆盖矩阵 `N2.1-14` 去标注 → 纯 ✅；`N2.1-07` 标注编号 `①`→`②`（官方确认属第 ② 条） | N2.1-07 / N2.1-14 | — |
| R-14 | 待定枚举 token 归一为 **`tbd`**（F-10）：§C.2 增规范声明、§F 增 R-14 行；`moat_state`（§C.2 表）`tdb`→`tbd`；跨文件一致性声明补一行 | N2.2-02 | 术语一致性 |
| R-16 | §C.1 `price_gap_state` 取值**中文→英文**（`fully_reflected`/`not_fully_reflected`/`attractive`），同步 `classify_opportunity` 等值判断 | N2.1-02 | C-1（F-11） |
| R-18 | 待定项① / §F：allowlist `tier=free_quota`→**`license_tier=free_quota`**（`tier` 保留给来源分级） | N2.1-14 | C-3（F-11） |
| R-15~R-21 | §C.2 增「**命名规范**」声明（英文小写+下划线 / 禁中文取值 / 同名消歧 / 一名一概念）；§F 增 R-15~R-21 行（**R-17 当轮保留**给 C-2/C-2b；后于同轮回改中执行，见下行 R-17 行） | N2.2-02 | C-1~C-6（F-11） |
| R-17 | §C.2 增「**`claim` 三字段规范**」声明；§F R-17 行状态更新为 `✅ 已执行`（拆三字段：`claim_type`→`claim_form`；官方一手三分 `claim_nature`→`official_claim_kind`；"事实/计划/预测/解释" `claim_nature` 不变） | N6.2-03 / N6.2-04 / N9.1-13 | C-2/C-2b |
| R-22 | ① §F R-17 行补目标文件/节号；② `claim.nature` 点号漂移 → **`claim_nature`**（04/01 §2.2(b)、04/02 §E.2）；③ 06/01 §2.3(a) 输出列补 `claim_form`/`official_claim_kind`；④ 09主 文件头版本行补 R-17/R-22、本文件头同步为 `R-01~R-22` | N4.2-01 / N6.3-02 | C-2/C-2b（R-17 收尾） |
| R-23 | 全包版本行「概括 + 指针」归一（明细见 §F R-23 行）：13 处非 owner 文件版本行**删除 R 编号枚举**、本文件头同步为 `R-01~R-23`；§C.2 增「留痕规范」④ | 留痕规范 | F-10 族（留痕） |
| R-24 | §B.1 增 L1 作用域声明（断言仅对 `Recommendation` 字段名，非全仓扫描）；§B.2 P-04 行消歧（`position` 不约束 `position_listed`/`business_positions`）；§B.3 Checker-2 加作用域注释、禁改全仓 token 遍历；§B.1 后新增「**★ 禁词作用域通则**」（入库前 grep / 有同名必声明 scope / scope 须可执行）；文件头版本行同步为 `R-01~R-24` | N2.1-03 / N2.2-06 | P-09 同族（scope 消歧） |
| R-25 | 覆盖矩阵：① 新增「覆盖状态」图例行（⚠️ = **已收口 · 待收尾**，不阻塞交付）；② `N2.2-05` 状态列 `⚠️ N 待冻结` → `⚠️ **已收口 · 值可配置待冻结**（N，基线值 B1 = 2）`；③ `N2.1-07` 去「待澄清②」→「A4 已确认」；④ 统计行归正 + 新增基线值指针（指向 `00_待拍板项清单` D-03/D-04）；⑤ §F 增 R-25 行、文件头版本行同步为 `R-01~R-25`；⑥ §技术待定项（第二章）②–⑤ 状态列 `需需求方澄清` → `✅ 已确认（D-03 · 2026-09-15）`，表头说明与结论行同步 | 跨章口径一致性 | D-03 / D-04 |
| R-26 | §B.4 配置扫描范围扩展：正文表下方新增「被检目标清单（含新层配置）」说明 + mermaid `SRC` 子图增 4 节点（trigger / notification / freeze / delivery 及三入口展示配置）；§B.1 L3 行加扫描范围指针（三处一致，不冲突） | N8.3-05 / N8.1-09 / N10.3-08 / N11.2-12 | S-01 / S-02 / S-03 / S-15 |
| R-27 | §B.2 P-09 行加注 + §B.3 Checker-1 `is_decision_scope` 增「**纳入 / 排除三类调整**」（纳入通知 / 每日页排序；排除复盘标记字段 `eval_layer`/`error_axis`/`error_axis_note` 与 `freeze_param` 读取）+ §B.1 L2 行同步（三处一致） | N8.1-07 / N8.1-09 / N10.3-12 / N11.2-12 | P-09 同族（scope 消歧） |
| R-28 | §B.1「★ 禁词作用域通则」第 4 点新增**第 3 实例**「检查器命名空间免扫」（`scripts/checks/**`、`tests/**` 内免扫），"两次实例"→"**三次实例**"；§B.3 Checker-1 `scan_ast` 补**可执行豁免**（`is_checker_namespace(path) → continue`，置于 `ast.parse` 之前）+ 注释块补第 4 条「免扫」（使 scope 满足通则第 3 点"必须可执行"，避免扫到检查器自身而卡死 CI）；只做消歧、不新增禁止项、不放松约束 | 跨章口径一致性 | P-04 / P-09 同族（scope 消歧） |
| R-29 | 09 新增 §3.3.5 `eval_result` 固定字段表（`eval_layer` / `error_axis` / `error_axis_note`，标注"标记字段、不进决策函数"）；**已执行**（eng-2） | N10.3-01/12 | S-04 |
| R-30 | 4 份 PM `01_需求拆解.md`（`02/01` · `01/01` · `08/01` · `03/01`）各 §5 T 映射表逐条回改：T12 两处回源稿原场景（证据撤回 / 财务数值重述；"基准口径变更"另立专项）、T13 补"或来源"、T10 回源稿口径并移除"未研究"、T03 补"不自动升级"、T09 `/`→`或`（精度提示）；**已执行**（eng-3，各文件回改记录有痕） | N10.2-02 | C10-3 / 🟡×4 |
| R-31 | 三份 `02_实现方案.md`（`08_产品入口与每日运行` / `10_验收与持续复盘` / `11_交付顺序与实施参数`）「建议回改项」**节标题 + 说明句 + 段末「说明」句**归正：① 标题「（待主理人统一登记）」→「（**已收口 · 登记于 `02/02 §F` R-26~R-30**）」；② 说明句改为"所列项已于收口轮按三段式审计链回改执行，编号见 `02/02 §F`（**唯一权威登记**）"，保留原「格式：」行；③ **段末「说明」句**由"建议主理人…统一登记（新编号续接）"改写为完成时"已由主理人在收口轮统一登记为 R-26~R-30 并执行完毕…；本表「现状 / 改成什么」保留原始登记内容供追溯"（按各文件对应 R 号：`08/02` R-26/R-27；`10/02` 第 1 条 R-29、第 2–3 条 R-27/R-26、第 4–5 条 R-30；`11/02` R-26/R-27）；三份文件原表一行未删、其它内容未动 | 跨章口径一致性 | S-01 / S-02 / S-03 / S-04 / S-07 / S-15 |
| R-32 | `01_产品目标与核心闭环` 两份：`01/01` §2.2 承接表第 7/8 步 ❌缺口 → ✅已拆解（`08/02` / `10/02`）+ 表下加注 + 「其它跨章缺口」两条改"已承接" + §4 阶段表"属待拆章节"→"由第八 / 第十章承接" + §6 交接要点第 6 条改"已由第八 / 十章承接"；`01/02` §D.1 8 步状态表第 7/8 步同改 + 编排器职责"拆解后即插即用"→"已拆解、即插即用" + §H 接口"（未拆解）"→"（已拆解 · `08/02` / `10/02`）"；两文件版本行/回改记录同步；**已执行**（eng-3，两文件回改记录有痕） | N1.2-01 | 交付可读性（过时表述） |
| R-33 | §F R-32 行节号笔误订正（`§B` → `§D.1`，共 5 处：`02/02` §F 3 处 + `02/02` 文末 R-32 行 1 处 + `01/02` 回改记录 1 处）；§F 新增本行；文件头版本行同步为 `R-01~R-33` | 留痕规范 / N10.2-02 | F-10 族（留痕） |
| R-34 | 15 份非 owner 文件版本行归一：删除全部 R 编号子句（`；R-25…` / `；R-30…` / `；R-29…` / `：R-25…；R-31…；R-33…`），只留「（2026-09-15 回改）」概括 + 文末回改记录指针，行内原有其它信息（待定项状态 / 需求统计 / 含 D-01 等）保留；owner 文件 `02/02` 版本行按 R-23 豁免（保留「`R-01~R-XX 全部执行，见 §F`」形态，区间推进至 R-34） | 留痕规范 | R-23 回归（F-10 族） |
| R-35 | ① 新建根级 **`00_交付索引.md`**（11 章成果导航 + 全局件 + 按目的阅读分流 + 命名与留痕规范速查 + 上游源件区分）；② 新建根级 **`00_交付施工图.md`**（Ch11 五阶段施工主线 + 逐阶段施工清单 + 全局组件总表 + 目录地基 + 关键路径与并行 + 工作量汇总 + 施工前待核 **N-01~N-05**）。两文件均为新增件，各自含版本行 + 文末回改记录；**未改动任何既有文件** | 交付实施准备（收口后新增件） | N-01（T 权威表指向） |
| R-36 | 新建根级 **`00_开发Agent开工提示词.md`**（**自包含**的开发 Agent 开工提示词：身份与三条底线 / 禁写代码先建认知 / 工程根 `/Users/gaza/Developer/InvestSigh` 与 18 JSONL 地基 / 五阶段门禁与并行组 / 任务 SOP（DoD 先行 + 证据先行）/ 四种"假交付"判定 / 施工纪律 12 条 / `Phase N` 集成硬化 / 独立验收审计 / 待核 N-01~N-05 / 交付前 10 问），含版本行 + 文末回改记录；**未改动任何既有文件**（仅本 `02/02` 按 owner 职责登记 §F 与版本行） | 交付实施准备（收口后新增件；2026-09-15 需求方指令） | N-01~N-05 |
| R-37 | 运行环境 **Windows → macOS**，全包工程根路径同步（**4 文件 13 处**）：① `00_开发Agent开工提示词.md`——§一 配套路径改为"需求对齐包根 = 本文件所在仓库根（`git rev-parse --show-toplevel`）"；§三 加"运行环境 = macOS"首句 + 工程根与目录树改 `/Users/gaza/Developer/InvestSigh` + 两条 ⚠️ 改写（"不要动 `00_*` 与 `01_`~`11_`"、父目录警告改 `/Users/gaza/Developer/`）；§五 `AC-07` 与 §八 `N-2` 解除对本机 skill 路径的依赖（补"本地没有就自己写一个" + 检测规格与退出码约定）；§八 `N-2` 前一句 `code_root` 改绝对路径；§十一 第 3 步同步；② `00_交付施工图.md`——§2 阶段① 骨架行 + §4 工程根与目录树；③ `09_数据与实现约束/09_需求拆解与实现方案_v1.md §3.3.3` 目录布局首行；④ 本文件 §F 与文末 R-36 行内示例路径。**未新增任何门槛、未改任何策略口径** | 交付实施准备（环境迁移） | 无 |
