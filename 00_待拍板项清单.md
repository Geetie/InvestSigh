# 待拍板项清单（v1 · 2026-09-15）

> **用途**：把"只能由需求方决定、实现方不能自行拍板"的事项一次列全，供集中拍板。
> **来源**（三处合并去重）：① 源稿第十一章「文档确认时需要冻结的参数」（**11 项，权威骨架**）② 第一~九章实现方案中标注"**需需求方澄清 / 裁定**"的项 ③ 各章「技术待定项」表里需要业务侧给方向的项。
> **计数**：**A 档 9 项**（必须拍板）· **B 档 22 项**（需给口径）· **C 档 20 项**（实施参数，随第十一章冻结，不需逐项拍）· **已拍板 2 项**。
>
> **共同纪律（源稿第 386 行红线）**：所有待定项在拍板前，实现方**保持可切换**；**不得把待定项悄悄补成新的策略门槛**。

---

## A 档 · 必须由需求方拍板（9 项）

> **判据**：影响**投资主线或规则语义**——不定死，实现方无法判断自己做得对不对，也无法验收。
> 这 9 项里有 5 项（A1–A5）就是各章反复出现的"**跨三章既有待澄清 4 条**"（A1 与 A2 同属第 ⑤ 条），它们已在第一、二、三章的"技术待定项"中**逐字一致地列了三遍**。

| # | 待拍板项 | 备选口径 | 不定会怎样 | 来源锚点 |
|---|---|---|---|---|
| **A1** | **主基准 ETF 最终冻结**（是否 AGIX） | ① AGIX（源稿建议，理由：需求方已有持仓）② 换别的 | 相对收益的分母定不下来，"跑赢 / 跑输"就**没法算** | 源稿 11-① · `03/01` N3.4-06 · 跨三章 ⑤ |
| **A2** | **收益口径三件套**：信号生效价 / 交易成本 / 税前口径 | 待定 | 回测与复盘的数字互不可比；同一策略换个口径结论能翻过来 | 源稿 11-⑩ · `03/01` N3.4-06 · 跨三章 ⑤ |
| **A3** | **非美证券是否进入首版推荐范围** | ① 仅美股（当前默认）② 含非美 | 首批**可推荐标的的集合**不定。已做成参数 `recommended_security_scope`，等值即可切换 | 源稿 11-③ · `03/01` B-17 · 跨三章 ④ |
| **A4** | **官方确认 `confidence_boost` 是否可能演变为门控 / 阈值**（规则 7） | ① 纯加成，**永不参与任何比较**（当前默认）② 允许演变成门控 | 若它其实是个门槛，就直接违反第二章"**无固定置信度门槛**"红线 | `02/02` 待定项 ② · 跨三章 ② |
| **A5** | **决策门是否含"置信度数值阈值"**（C123-5） | ① gate 只做**结构完备性校验**、无数值阈值（当前默认）② 允许数值阈值 | 直接决定 gate 是否合规——这是"门槛 vs 排序"纪律的核心 | `02/02` 待定项 ③ · 跨三章 ③ |
| **A6** | **发布与人工复核政策**：默认自动更新 vs 人工批准 vs 对外授权的边界（C8-1） | 建议按**对象分层**：<br>· 常规研究/图谱更新 → 默认自动<br>· 基准变更 → 人工审批<br>· 对外公开发布 → 逐项显式授权<br>· 重大建议变化 / 争议推断 → 自动发布但标 `published_as=inferred` | 每天自动跑出来的结论**能不能直接用**，没有答案；源稿 11-⑧ 也把这条留白了 | 源稿 11-⑧ · `08` C8-1 · `03/01` N3.4-09 · `09` 张力 C5 |
| **A7** | **提问是否走完整 gate + 资源排队规则**（C8-2） | ① 走与自动监测**相同的证据门槛**（当前默认）② 事实类问题短路到"证据检索"分支 | 用户提问产出的东西**算不算建议**，口径不定；已做成 `rules/ask.yaml:gate_mode` 两模式可切换 | `08` C8-2 · `08/01` S-04 |
| **A8** | **负收益但相对领先时，既有积极建议如何处置** | 待样例校准 | "预计跑不赢 → 卖出"**已确定**；但"**已经持有的积极建议**撞上这种情况怎么处理"没定——这是个真实的策略缺口 | 源稿 11-⑥ |
| **A9** | **通知与分享**：展示入口 / 提醒渠道 / 多人访问权限 / 外部分享方式 | 待产品设计确定 | 第八章已把 `notification_channels` / `notification_aggregation` / `publish_policy` **全部留成 `tbd`**（接口先定、值待定） | 源稿 11-⑪ · `08/02` §K ③ |

---

## B 档 · 需你给个口径（22 项，有建议值，确认即可）

> 这些**不改投资主线**，但不定会让实现方"做出来但不知道合不合格"。分五组。

### B-1 阈值类（需定数值，6 项）

| 项 | 事项 | 建议 | 来源 |
|---|---|---|---|
| B1 | **反拖延阈值 `N`**（horizon 连续延长几次算可疑） | 建议 **2** | `02/02` N2.2-05 |
| B2 | **价格下跌强制复查触发阈值** | 先占位保守值；**明确非止损门槛** | `02/01` N2.1-11 · `05/02` J6 · `07/02` J8 |
| B3 | **传导放大上限 `amplification_cap`** | 占位保守值，参数化于 `rules/transmission.yaml` | `07/02` J4 |
| B4 | **未建模覆盖率阈值**（低于它禁止称"完整预测"） | 参数化 | `05/02` J10 |
| B5 | **形式完备性最低阈值**（非空率 / 定位数 / 推导数） | 占位 + 参数化 `rules/baseline.yaml` | `04/02` J3 |
| B6 | **关键驱动上限默认值** | 建议 **5**（可配，超限分级） | `04/02` J2 |

### B-2 规范类（需定"怎么算合格"，7 项）

| 项 | 事项 | 建议 | 来源 |
|---|---|---|---|
| B7 | **护城河机制表述规范** | 定义"机制必含：保护对象 + 维持投入 + 失效条件" | `04/02` J5 · `04/01` N4.3-01 |
| B8 | **代际划分粒度** | 与 `products.generation` 对齐 | `04/02` J6 |
| B9 | **反证完整性规范** | 待定义 | `07/01` N7.4-08 |
| B10 | **无催化剂时的表述规范** | 待定义（不得暗示买卖） | `07/01` N7.4-10 |
| B11 | **`probability` 何时才能填的标准** | 须为 `DerivedValue` 且标注来源；否则默认 `null`（不是 50/50） | `05/02` J9 |
| B12 | **每日价格解释的"解释充分性"与三档判定标准** | 待定义 | `05/02` |
| B13 | **每类估值假设的离散粒度** | 参数化 `rules/valuation-methods.yaml` | `05/02` J2 |

### B-3 语义边界类（4 项）

| 项 | 事项 | 来源 |
|---|---|---|
| B14 | **业务 vs 产品线的边界**怎么划 | `07/01` N7.1-01 |
| B15 | **"局部关系"的默认跳数范围** | `07/01` N7.2-07 |
| B16 | **什么算"同一事件"**（事件指纹的时间桶粒度） | `09` J7 · `06/02` K2 |
| B17 | **服务类业务的"代际"字段怎么填** | `07/01` N7.1-05 |

### B-4 展示与人工介入类（3 项）

| 项 | 事项 | 来源 |
|---|---|---|
| B18 | **估值解集展示上限 `N`**（反解多解时展示几组） | `05/02` J3 |
| B19 | **叙事价值的量化方式** | `05/02` |
| B20 | **人工介入率与兜底触发条件**（独立判定 / 半可计算主张 / gap 升级） | `06/02` K4 / K6 / K9 / K10 |

### B-5 合规与运维类（2 项）

| 项 | 事项 | 来源 |
|---|---|---|
| B21 | **脱敏清单**（对外分享时脱哪些字段） | `09` J8（待法务口径） |
| B22 | **RPO / RTO**（灾难恢复目标） | `09` J10（建议 ≤1h / ≤1 工作日） |

---

## C 档 · 实施参数（20 项，随第十一章冻结，不需你逐项拍）

> 这些是"实现方评估后给方案"的，你只需**看方案、点头**。列在这里是为了**不漏**。

**源稿 11 项里属于本档的 5 项**

| 项 | 内容 |
|---|---|
| C1 | **运行时刻 / 时区 / 非交易日**（已定"日度 + 事件触发"，具体待评估） |
| C2 | **可承诺时效目标**（上限 = 公开数据更新频率） |
| C3 | **公开数据与预算**：可用接口、模型与部署费用、检索预算 |
| C4 | **首批公司与产品关系的具体名单**（NVIDIA 核心生态已确认；名单待证据核验后附表，**不凑数量**） |
| C5 | **收益预测方法的具体算法**（个股与 AGIX 同期间同情景，通过样例确定） |

**其余 15 项**：每条建议期限的具体设定（**范围已定：一个季度到一年**，按论点设定、不得任意滚动）· 存储组合（文件真源 + SQLite 索引）· 备份远程位置 · 并发写策略 · 模型对 `rules/` 的权限（**硬要求只读**）· 依赖包版本锁定 · as-of 性能门槛 · 抽取 vs 推理模型分离 · 传导深度上限（建议硬上限 5）· 因果链可达性深度 · 环折叠取值 · 邻接表刷新策略 · 事件指纹参数（同 B16）· 三类预算默认值 · 人工核验样本库覆盖频率。

---

## 已拍板（对照，不用再议）

| # | 决策 | 生效 |
|---|---|---|
| **D-01** | iFinD / pandadata MCP 连接器**有免费额度，暂不视为"付费数据订阅"** | 2026-09-15 |
| | → 可进首版可用清单（标 `license_tier=free_quota` / `cost=0`），但**不得写入裁决逻辑**；若未来转付费，须按 N2.1-14 重新评估 | |
| **D-02** | `claim` 三套分类**拆为三个独立字段** | 2026-09-15 |
| | → `claim_nature`（认识论性质）/ `claim_form`（来源形成形式）/ `official_claim_kind`（官方一手披露性质，仅 `tier=primary`）。三者维度不同，**不得互换、不得合并** | |

---

## 建议的拍板顺序

1. **先拍 A1–A5**（跨三章那 4 条）——它们是**相对收益的算法地基**，不定死，第四、五、七章的验证都做不了。
2. **再拍 A6–A7**（发布与提问的门控政策）——决定第八章的每日自动化能不能直接落地。
3. **A8–A9** 可以和产品设计一起走。
4. **B 档**可以整表交给实现方：每项已带建议值，你只需"同意 / 改成 X"。
5. **C 档**不需要你现在拍——实现方在第十一章会给出评估后的方案。

> **若这些参数最终由老板/客户确认**：A 档 9 项 + B 档表格可原样提交，每项都已写明"不定会怎样"，便于对方快速判断。
