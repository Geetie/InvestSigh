# --- AI 工具目录（与 FastInvest 仓库既有习惯一致：不收录 .workbuddy 快照）---
.workbuddy/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- 临时产物 ---
*.tmp
~$*
*~

# --- 运行时状态（不进版本库；每次运行重写，可重建）---
system/state.json
# 夹具**排他会话锁**（`G-RC-10`）：运行态文件，只对"同一工作树内的并发 pytest 会话"有意义。
# ★ 它刻意放在 `system/tests/.work/` **之外** —— 放里面的第一版会被 `_clear_work_dir`
#   自己删掉，导致互斥失效（详见 `system/tests/conftest.py::_SESSION_LOCK` 的注释）。
system/tests/.pytest-session.lock

# ★★ 关于 `system/derived/`：**它是真源，必须入库**（此结论经独立审计证伪后更正，缺口 `G-48`）。
#
# 我曾在这里写过"derived/ 可由 facts/ + method_version **确定性重算**，故非真源、不进版本库"，
# 并据此忽略 `system/derived/*.jsonl`。**该前提是错的，已撤回**：
#   ① 计算层**自己的**写入方逐字称其为追加式真源 ——
#      `scripts/compute/store.py`：「`derived/derived_values.jsonl` —— `DerivedValue` 的**追加式真源**」；
#   ② **"可确定性重算"实测不成立**：`scripts/compute/contract.py` 里
#      `computed_at = computed_at or datetime.now(timezone.utc)`
#      ⇒ 同一输入两次运行的输出**逐字节不同**；且 `version` 来自 **CLI 参数**、不来自 `facts/`；
#   ③ `derived/` 承载的正是 `G1-03` 四要素里的**「计算」**那一项（结论可追溯到
#      evidence / assumptions / **computation** / prev_version）——
#      **不版本化它，等于让四要素之一在审计时只能靠"再算一遍"，而"再算一遍"已被 ② 证否**。
#
# ⇒ 故**不再忽略** `derived/` 下的 JSONL（`.gitkeep` 照旧跟踪）。
# ★ 遗留缺口（已登记，**未修**）：`append_only_guard.py` 的 pathspec 仍只有
#   `system/facts/*.jsonl` —— 即 `derived/` 虽是追加式真源，却**还没有**"既有行不可改"的守卫覆盖。
#   我**不**把这条当作"derived 不是真源"的证据（那正是我上次犯的推理错误：
#   把"守卫没覆盖"读成"性质不成立"）。修法见缺口台账。

# --- 并行工作树（agent 各自的工作副本；成果经主控收集后并入 main，目录本身不入库）---
.worktrees/

# --- 主控用的临时合并/诊断目录 ---
.merge_tmp/
