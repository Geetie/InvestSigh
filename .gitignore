# --- AI 工具目录（与 FastInvest 仓库既有习惯一致：不收录 .workbuddy 快照）---
.workbuddy/

# --- OS / IDE ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- 临时产物 ---
*.tmp
~$*
*~

# --- 运行时状态（不进版本库；每次运行重写，可重建）---
# 真源是 facts/ 与 derived/ 下的 JSONL；state.json 只是 resume 用的运行态快照。
system/state.json
