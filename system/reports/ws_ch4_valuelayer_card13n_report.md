# 卡 13-N 报告：`verify.py` 散文里的「分片事实」单源化

| 项 | 值 |
|---|---|
| 卡 | 13-N（主理人 2026-09-16 指派） |
| 工作树 | `.worktrees/ws-ch4-valuelayer`（分支 `ws/ch4-valuelayer`） |
| **开工基线** | `ed9687b23f18f4b2b03a497e35a3aaea3603bb89`（= 当时的 `main`，已 `git merge main --ff-only` 对齐） |
| **交付基线** | `7917ea9`（= `main` `0346b87` 的合并点）；首个提交 `10b6de1` 的基线是 `56fd637` |
| **分支尖端** | `947ebf2`（= 再并入一个 **docs-only** 提交 `0346b87` 的合并点；`main` 是它的祖先 ⇒ **0 落后**）。全部验证读数都是在 `7917ea9` 基线上取得的；其后只有 `batch13_taskbook.md` 的文档改动，**不影响任何判据**。 |
| **合并次数 / 冲突数** | **2 次合并**（`G-61` 幽灵门禁的持久形态 ×1 + `main` 改了同一文件 ×1）；`verify.py` 冲突 **1 处**（见下） |
| 改动对象 | `system/scripts/ops/verify.py`（**散文**：模块 docstring + 注释） |
| 新增 | `system/tests/guards/test_verify_prose_binding.py`（守卫 + 反向对照） |
| 改后校验 | `pre-commit` **14 门禁 / 14 PASS / RC=0**；`git status` 除本卡 4 个文件外为空 |

★ **两次合并都撞在同一件事上**：`main` 在本次交付期间**两次**改动 `verify.py`，而两次都**正好落在我改的那几段**：

1. **第一次（`ed9687b`→`56fd637`）**：`main` 已经"修"过本卡的题眼 —— 它把表里那行从
   「`injection-a`…`injection-f` ＋ **6 个分片**」改成「`injection-a`…`injection-g` ＋ **7 个分片**」，
   即**把数字改对了**。这是**唯一一处冲突**。我**取自己那一侧**（删掉枚举与片数，改指真源），
   理由正是本卡守卫的红字告警：**"不要把数字改对 —— 改对了仍然会再漂移一次"**
   （下一次开 `injection-h` 就会再次过期）。主理人那句「分片那行写 `6 个` 而**实际已 7 个**」也随之
   改为「而常量早已新增片」，否则它自己就成了**第三个存放处**。
2. **第二次（`56fd637`→`0346b87`）**：`main` 的 `96d4d7e` 把 `unit` **120s→300s**、
   `injection-g` **90s→300s**（口径 21：两个仪器可差 16×）。这**推翻了我写的"取值理由"** ——
   我原先在 `unit` 沿革末尾写「现取值来自修后 15.79s/11.42s ⇒ ≈7.6×」，而 `main` 明确说那两个数
   是**裸 pytest 墙钟、不能用于定值**。⇒ **我改口 3 处**：沿革**只留历史、不再为当前值给理由**
   （理由交回参数行尾注释）；`injection-g` 删掉已过期的"按分片规则的下限起步"；
   injection 分片块删掉"各片不必然同值 —— g 是起步值"。
   ★ 这一次没有冲突（git 自动合并），**是我自己主动复核发现的** —— 也正是 `口径 11`（merged ≠ closed）
   要防的事：**合并成功 ≠ 合并后的散文仍然为真。**

★ **本卡的口径（先声明，因为它决定了每一处的取舍）**

| 类 | 内容 | 处置 |
|---|---|---|
| ① | **片数**（`6 片` / `六片` / `6 个分片` / `6 个轮次`） | **删除**（真源 `INJECTION_SHARDS` 的长度） |
| ② | **分片枚举上界**（`injection-a`…`injection-f`） | **删除**（真源 `INJECTION_SHARDS`） |
| ③ | **各片用例数**（`a27 b28 c27 d29 e32 f29`、`本片 27 → 30`、`injection-b（28）`、`共 165 例`） | **删除**，改指现算命令（真源 = 目录本身） |
| ④ | **每片用例数上限**（`≤32` / `上限 32`） | **删除**，改指 `test_shard_coverage.py::MAX_CASES_PER_SHARD` |
| ⑤ | **批次超时值**（`取上限 300s`、`270s`） | **删除**，真源 = 各批 `Batch.timeout`（13-F 已为 docstring 表立过此例） |
| ⑥ | **带日期 / 带「实测」的读数**（`48.73s`、`count:102044`、`271→275 项`、`169 例/290s`） | **保留** —— 它们是取值决定的**证据**，不是真源的副本；删了反而丢证据 |

★ ⑥ 与 ①~⑤ 的分界就是本卡的判据：**「描述当前事实」的数字必然漂移 ⇒ 删；「记录某次实测」的数字不会漂移 ⇒ 留。**

---

## 一、改了什么（逐处，共 21 处 + 1 处新增声明段）

★ **交付后自查又抓到 2 处同类漏网**（`injection-a..f` / `a..f`），并为此补了**第 ④ 条判据** ——
逐处原文与证据见 **§五**。下表仍是首轮那 21 处（行号以 `ed9687b` 为准，**已不再与 HEAD 对齐**）。

行号均指**改前**的 `ed9687b`。

### 1. 模块 docstring

| # | 改前行 | 改前原文（逐字） | 处置 |
|---|---|---|---|
| 1 | `:29` | `` \| `injection-a`…`injection-f` \| `tests/injection/` 的 **6 个分片**（每片一组显式文件路径） \| `` | ①+② 改为 `` \| `injection-*`（**片名单不在本表重复**） \| `tests/injection/` 的分片（每片一组显式文件路径） \| `` |
| 2 | `:38` 后 | —（原文无此段） | **新增**「注入分片的名单/枚举上界与片数同样不在本表重复」段，并给出查真值命令 —— 与既有「超时值不在本表重复」段并列，把两类漂移绑到同一课 |
| 3 | `:43` | ``## ★ 为什么 `tests/injection/` 必须切成 6 片（**门禁曾被静默关掉**）`` | ① 删数字 ⇒ `必须分片` |
| 4 | `:70-73` | ``片大小取 **≤32** 的理由是：① 主理人的**授权上限**；② 有**实测支持**／（27 / 28 例的片在本机当前阈值下单轮跑完、`exit=0`）。`` / ``按 `9999 ÷ 785 ≈ 12` 例/片重排（≈14 片）——`` | ④ 删 `≤32`；③ 删 `27 / 28 例`；① 删 `（≈14 片）`。改写为「上限值不在本文件重复，真源 = `MAX_CASES_PER_SHARD`」 |
| 5 | `:91` | ``完整覆盖 `tests/injection` 现在需要 **6 个轮次**（每轮跑一片）。`` | ① 改为「需要**逐片各跑一个轮次**（片数 = `INJECTION_SHARDS` 的长度，**不在此处重复**）」 |
| 6 | `:94` | ``` `--batch all` 会把六片连着跑完 ``` | ① 改为「会把所有分片连着跑完」 |

### 2. `INJECTION_SHARDS` 上方注释块

| # | 改前行 | 改前原文（逐字） | 处置 |
|---|---|---|---|
| 7 | `:226` | `# 为什么分 6 片（**不是设计选择，是宿主配额决定的**…）` | ① ⇒ `# 为什么分片（…）` |
| 8 | `:227` | ``#   `tests/injection` 共 165 例 × 每例一份夹具副本 ≈ 44,715 项/轮 ≫ 宿主单轮配额。`` | ③ 删（改为「每个用例各带一份夹具副本 ⇒ 单轮总量 ≫ 配额」+ **现量命令** `pytest tests/injection --collect-only -q`） |
| 9 | `:228` | `#   完整覆盖该目录现在需要 **6 个轮次**（每轮一片）。` | ① 同 #5 |
| 10 | `:237` | `#        6 片（每片 ≤32 例）在 **9,999** 这个较低阈值下**不成立**（需 ≈14 片）；` | ①④ ⇒ `**当前片数**（每片都接近用例数上限）在 **9,999** 这个较低阈值下**不成立**；` |
| 11 | `:238` | `#        维持 6 片是"按观测到的较高阈值 + 风险登记"的口径…` | ① ⇒ `维持当前片数是…` |

### 3. `BATCHES` 字典

| # | 改前行 | 改前原文（逐字） | 处置 |
|---|---|---|---|
| 12 | `:253` 前 | —（原文无此段） | **新增**「读本字典前必读」段：**唯一真源 = `Batch(...)` 的位置参数**；各批注释里的秒数一律是**取值沿革/留档**；并逐条点明「片名单 / 每片用例数 / 上限」的真源各是谁。这一段是本卡对**第 5 类漂移（超时值复述）**的收口方式：不逐个删（会掏空 `V-02` 的取值理由），而是把它们**统一降格为留档并声明真源** |
| 13 | `:255` | `# ★ 超时 60s → 270s（主理人实测后上调，批次 13 集成期）。` | ⑤ 改为「超时由 60s 上调（**真值 = 本行 `Batch.timeout`**；散文不重复它）」 |
| 14 | `:270-274` | ``# ★ 270s 是**故意保留的安全网**（修后实测 15.79s ⇒ 17.1×…）`` … ``#     参考值：15.79 × 8 ≈ **126s**（即 120s 是"确认后可取"的值，不是现在就该取的值）。`` | ★★ **这是一处活的自相矛盾**：散文说「现在是 270s、120s 是将来的值」，而同批参数是 **`120.0`**（`:276` 行尾注释还写着「120s ≈ 7.6×」）。⑤ 改为「取值沿革」：`60s → 270s → 现取参数里那个值`，把"当前值"的唯一承载者交回参数 |
| 15 | `:292`（`guards`） | ``#   取值依据 `V-02`（以较慢一次 101.96s 为基准；4× = 408s 越 300s 上限）⇒ **取上限 300s**。`` | ⑤ **保留未改**（与参数 `300.0` 一致、无矛盾）—— 由第 12 项的声明段统一降格为留档。**这是本卡刻意的边界：修「与真源矛盾」的散文，不逐个重写「与真源一致但重复」的散文。** |
| 16 | `:297` | ``# ── `tests/injection/` 的 6 个分片（**原 `injection` 目录全量批次已删除**）──`` | ① ⇒ `的各分片` |
| 17 | `:302-306` | ``# ★ 超时：各片**互不相同**、按**各自实测**取值…`` / ``#   取值规则 = **实测 × 8**…：a/e 90s · f 120s · b/c 150s · d 210s。一律 ≤300s。`` | ⑤ 删整段取值枚举（**"各片互不相同"已是假陈述** —— 现值 a–f 都是 300s、g 是 90s）⇒ 改为「真源 = `Batch.timeout`，散文不再抄一份」+ 查真值命令；**保留**隔离副本实测秒数（⑥） |
| 18 | `:313` | `#   ★ **最终取值 = 六片统一 300s**（`V-02` 允许的上限）；理由与上面那条相反方向的取舍：` | ①⑤ ⇒ 「按 `V-02` 取不超过上限的最大值（真值见各批 `Batch.timeout`；各片不必然同值 —— `injection-g` 是起步值）」 |
| 19 | `:324` | `#     ① 作为**故障探测器**，300s 对**工作树实测**是 2.7~4.7×：` | ⑤ ⇒ 「**本批的上限**对工作树实测是 2.7~4.7×（上限值见 `Batch.timeout`，不在此处抄）」 |
| 20 | `:327` | `#   （"写了却永远不被验证"），按设计生效。本片 27 → **30**（仍 ≤32）。` | ③④ ⇒ 「本片用例数随之增加（**现值不在此处写死**…）」 |
| 21 | `:347` | ``# `test_guards_reject_a.py` 是原 `test_guards_reject.py`（41 例）的**前半 21 例**：`` | ③ ⇒ 删 `41 例` / `21 例`（拆分记录本身保留在下一行的 `≈ 11,111 项` 上，那是 ⑥ 类实测） |
| 22 | `:359` | `#   （`test_idempotency_rows.py` 6 → 9），本片因此涨到 **34 例 > 上限 32** ——` | ④ 删 `上限 32`（`34 例` 是那次红的**实测证据**，属 ⑥ ⇒ 保留）⇒ 「涨到 **34 例**、**越过每片用例数上限**」 |
| 23 | `:362` | `#   ⇒ 现为 a27 b28 c27 **d29** e32 f29，合计 172，全部 ≤32。` | ③④ 整句删除 ⇒ 「此后各片的用例数**不在此处写死**（`G-28`）…现算 + '≤ 上限' 的断言都在 `test_shard_coverage.py`」 |
| 24 | `:383-386` | `#   ★ 为什么**不能**塞进既有分片：现为 a27 b28 c27 d29 e32 f29，**最大余量只有 5 例**，` / `#     而本文件 **17 例** ⇒ 任何既有片都会 > 上限 32 ⇒ 只能**新开一片**。` / `#   ★ 超时 90s：按 13-F 给分片定的规则…；17 例量级与本片` / `#     `injection-a`（27 例 / 6.85s 隔离）同阶 ⇒ 90s 起步，待 13-F 对照表重算。` | ③④⑤ 删 `a27…f29`、`5 例`、`上限 32`、`超时 90s`、`27 例` ⇒ 「当时各片的余量都装不下本文件」；保留 `17 例`（⑥，那次 `V-06` 变红的证据）与 `6.85s`（⑥） |
| 25 | `:494` | ``#     计数；其余为纯内存用例，`P-05`），与 `injection-b`（28）/ `injection-d`（29）同一量级。`` | ③ 删 `（28）`/`（29）`（**已过期**：现为 b=32 / d=29）⇒ 「与 `injection-*` 各片同一量级（各片现值不在此处写死）」 |
| 26 | `:505` / `:508` | `# 超时：工作树内**实测 170.15s**（191 passed）→ 按 `V-02` 取**不超过上限的最大值 300s**` / `本批可能被拖过 300s 而**假红**。` | ⑤ 删 `300s`（`170.15s`/`191 passed` 属 ⑥ ⇒ 保留） |
| 27 | `:516` / `:518` | ``# ★ `tests/injection/` 的 6 片必须**逐片单独跑**…`` / `#   在某一处突然变红（`V-08`）。要完整覆盖该目录，请分 6 个轮次跑。` | ① ⇒ `的各片`；`请**逐片各跑一个轮次**（片数 = INJECTION_SHARDS 的长度，不在此处重复）` |

### 4. 新增守卫

`system/tests/guards/test_verify_prose_binding.py`（10 用例，**零 `code_root` 夹具**）。
**刻意放在 `tests/guards/` 而非 `tests/injection/`**：① `tests/injection/` 的片是**显式文件路径**，
新文件必须登记进某一片，而落地当时 `injection-f` 已因用例数越限（33 > 上限）而红（见 §四）；
② `tests/guards/` 是**目录式目标**（`guards` 批），新文件自动被覆盖，`V-06` 与分片配额**零影响**；
③ 本文件只读一个源码文件，**零夹具副本 ⇒ 零宿主删除配额**（`P-05`）。

三条判据**全部从真源派生**（片名集合 = `verify.py::INJECTION_SHARDS` 现读，连"片字母"也由后缀现算）：

| # | 判据 | 覆盖的退化 |
|---|---|---|
| ① | 散文里不得出现**分片名单**（同一处 ≥2 个不同全名；单个片名是**交叉引用**，允许） | `injection-a`…`injection-f` 这类枚举上界 |
| ② | 散文里不得出现**片数/轮次**写法（`6 片` / `6片` / `6 个分片` / `六片` / `6 个轮次`；**惯用语「一片」刻意排除**） | `6 片` / `六片` |
| ③ | 散文里不得出现**旧分片数**写法（独立的 `<片后缀><1~3 位数字>`，如 `a27` / `e32`） | `a27 b28 c27 d29 e32 f29` |

判据 ③ 用 `(?<![0-9A-Za-z_])…(?![0-9A-Za-z_])` 边界，正是为了**不误伤夹具目录名里的十六进制片段**
（docstring 里就有 `…/test_L3_…-0a05b843`：`a05` 前一位是 `0` ⇒ 不算）—— 这一条已写进"反向的反向"用例。

### 5. 第二次合并后的 3 处改口（`main` 推翻了我的"取值理由"）

`main` 的 `96d4d7e` 把 `unit` **120s→300s**、`injection-g` **90s→300s**。我的散文里三处**理由**随之过期：

| # | 位置 | 改前（我写的） | 改后 |
|---|---|---|---|
| 22 | `unit` 沿革末句 | `→ **现取参数里那个值**（主理人裁决：修后 15.79s / 11.42s ⇒ ≈7.6×，落在 V-02 的 4~8× 内）。` | 沿革**只留历史**（`60s → 270s → 120s → 300s`，并注明 120s 那条依据**已被推翻**：裸 pytest 墙钟与门禁口径可差 16×）；**不再为当前值给理由**，改为「当前值的理由写在本行参数之后的注释里」（= `main` 新写的那段） |
| 23 | `injection-g` 批注释 | `…故按分片规则的下限起步，待 13-F 对照表重算。` | 删（已过期：g 现与 a–f 齐平）⇒ 「当前值的理由写在本批参数之后的注释里（**沿革与理由分开存放，别把旧理由当现值**）」 |
| 24 | injection 分片块 | `各片不必然同值 —— injection-g 是起步值，其取值理由见本批注释` | 删 ⇒ 「**各片是否同值也不在此处断言** —— 它随裁定时点变化」 |

★ 这三处**不是**"数字漂移"，是**"理由漂移"** —— 同一族里更隐蔽的一类：数字没写错，
但**它被归因于一个已被推翻的依据**。⇒ 建议并入 `G-28` 的表述：**"同一事实的第二存放处"
不只包括数值，也包括"对数值的解释"**。

---

## 二、测了什么（真实输出 + 退出码）

### 2.1 改前 / 改后清点（DoD ③）

清点脚本 `/tmp/n13/inventory.py`（四类判据、稳定排序、可直接 diff），**同一脚本跑改前改后**：

```
$ python /tmp/n13/inventory.py system/scripts/ops/verify.py
### 改前 SHA = ed9687b23f18f4b2b03a497e35a3aaea3603bb89
===== ①片数/轮次  （15 行）=====
  :43   [6 片]        ## ★ 为什么 `tests/injection/` 必须切成 6 片（**门禁曾被静默关掉**）
  :72   [14 片]       **若宿主阈值回落到 9999**，按 `9999 ÷ 785 ≈ 12` 例/片重排（≈14 片）——
  :91   [6 个轮次]     ★ **代价（如实写明）**：完整覆盖 `tests/injection` 现在需要 **6 个轮次**（每轮跑一片）。
  :94   [六片]        `--batch all` 会把六片连着跑完，因此它**不能**用来做 `tests/injection` 的完整覆盖。
  :226  [6 片]        # 为什么分 6 片（**不是设计选择，是宿主配额决定的**…）
  :228  [6 个轮次]     #   完整覆盖该目录现在需要 **6 个轮次**（每轮一片）。
  :237  [6 片][14 片]  #        6 片（每片 ≤32 例）在 **9,999** 这个较低阈值下**不成立**（需 ≈14 片）；
  :238  [6 片]        #        维持 6 片是"按观测到的较高阈值 + 风险登记"的口径…
  :313  [六片]        #   ★ **最终取值 = 六片统一 300s**（`V-02` 允许的上限）…
  :516  [6 片]        # ★ `tests/injection/` 的 6 片必须**逐片单独跑**…
  :518  [6 个轮次]     #   …要完整覆盖该目录，请分 6 个轮次跑。
  （另 4 行为惯用语「一片」：#300 / #307 / #328 / #384，**不改**，见 §一注）
===== ②枚举上界  （29 行：其中 1 行是缺陷）=====
  :29   [injection-a][injection-f]  | `injection-a`…`injection-f` | `tests/injection/` 的 **6 个分片** |   ← 缺陷
  （其余 28 行是 `INJECTION_SHARDS` / `ORDER` / `BATCHES` 的**真源字面量**与注释里的**单个**片名交叉引用）
===== ③旧分片数  （2 行）=====
  :362  [a27 b28 c27][e32 f29]       #   ⇒ 现为 a27 b28 c27 **d29** e32 f29，合计 172，全部 ≤32。
  :383  [a27 b28 c27 d29 e32 f29]    #   ★ 为什么**不能**塞进既有分片：现为 …，**最大余量只有 5 例**，
===== ④用例数/上限  （24 行）=====
  :69   [32 例]   ⇒ 故 `32 × 271 = 8,672 < 9,999` **不能**用来论证"32 例是安全的"。
  :71   [28 例]   （27 / 28 例的片在本机当前阈值下单轮跑完、`exit=0`）。
  :227  [165 例]  #   `tests/injection` 共 165 例 × 每例一份夹具副本 ≈ 44,715 项/轮 ≫ 宿主单轮配额。
  :237  [≤32 例]  #        6 片（每片 ≤32 例）…
  :327  [3 例]  :347 [41 例][21 例]  :359 [34 例][上限 32]  :383 [5 例]  :384 [17 例][上限 32]
  :385 [17 例]  :386 [27 例]  :494 [injection-b/injection-d]
  （其余为 ⑥ 类带日期实测读数，**不改**）
合计命中行数（含重复计一行多次）：70
```

```
$ python /tmp/n13/inventory.py system/scripts/ops/verify.py        # 改后
===== ①片数/轮次  （4 行）=====
  :308  [一片]  # ★ 每次**只跑一片**：同一轮里连跑多片会重新越过宿主单轮删除配额
  :315  [一片]  #   ★ 为什么取上沿（**本单实测教训**）：同一片 `injection-a` 在**工作树**里跑成
  :340  [一片]  #   ★ 给后续各流的固定约束：**新增注入测试文件必须同时登记进本文件的一片**，
  :399  [一片]  #     每片上限）⇒ 只能**新开一片**。（各片当时的用例数**不在此处写死**：现算与
     ⇒ 全部是惯用语「一片」，**无一处是片数声明**
===== ②枚举上界  （28 行）=====
  ⇒ 缺陷行 :29 已消失；剩余全是真源字面量与单个片名的交叉引用
===== ③旧分片数  （0 行）=====
===== ④用例数/上限  （16 行）=====
  ⇒ 去掉的正是 ①②③类（165 例 / ≤32 例 / 上限 32 / 27→30 / injection-b(28) / injection-d(29)），
    剩下的全部是 ⑥ 类**带日期或带「实测」字样**的读数
```

（完整改前快照：`/tmp/n13/before.txt`；改后：`/tmp/n13/after.txt`。）

### 2.2 语法 / 真源未被我改坏

```
$ python -c "…按路径 exec_module(verify.py)…"
OK  import 成功
INJECTION_SHARDS = ('injection-a', 'injection-b', 'injection-c', 'injection-d', 'injection-e', 'injection-f', 'injection-g')
ORDER 里片数 = 7
BATCHES 片数 = 7
```

★ **本卡只改散文，一个功能位、一个数值都没动** —— 这条不是声明，是**机器可核**的：

```
# 对象 = HEAD(= 56fd637):system/scripts/ops/verify.py  vs  我的工作树
（对象 = HEAD:verify.py vs 我的工作树）
AST（剥 docstring）相同 ?   True
剥「注释+docstring」后 token 流相同 ?   True
token 数 = 1895 / 1895
```

⇒ `INJECTION_SHARDS`、`ORDER`、`BATCHES`（含**每一个超时值**）、`_exit_zero`、`_stage_gate_verdict`、
`main()` 全部**逐字未动**；`git diff` 的 26 个 hunk 全部落在 docstring 与注释上。
（复核命令见 §4.8；`/tmp/n13/verify_head.py` 是 `git show HEAD:` 导出的对照版本。）

### 2.3 新守卫：正向 + 反向

```
$ python -m pytest tests/guards/test_verify_prose_binding.py -q -p no:cacheprovider
..........                                                               [100%]
10 passed in 0.52s          EXIT=0
```

**活体反向对照**（把已删掉的散文**逐条写回真文件** ⇒ 守卫必须立刻红；每次注入后校验 `sha256` 还原）：

```
$ python /tmp/n13/reverse.py          # 在交付基线 56fd637 上重跑过
基线 sha256(verify.py) = 7c72b7144b1e25c4…
基线（未注入）守卫：exit=0  1 passed, 9 deselected in 0.50s
✓ 注入 ①枚举上界        ⇒ 守卫 exit=1（红=True）判据号=①（期望 ①）
✓ 注入 ②片数（阿拉伯）   ⇒ 守卫 exit=1（红=True）判据号=②（期望 ②）
✓ 注入 ②片数（中文「六片」）⇒ 守卫 exit=1（红=True）判据号=②（期望 ②）
✓ 注入 ③旧分片数        ⇒ 守卫 exit=1（红=True）判据号=③（期望 ③）

还原后 sha256(verify.py) = 7c72b7144b1e25c4…  ✓ 与基线一致
反向对照结论：全部成立           EXIT=0
```

★ 基线与还原的 `sha256` 相同 ⇒ 反向对照**没有留下任何残留改动**。

★ **收尾补第 ④ 条判据后**（§五）：`11 passed`、**6 条**活体反向对照，基线 `sha256` 变为 `a5aa09676a86d6ca…`。

### 2.4 `guards` 批（含新守卫 + `main` 新带来的 `test_rule_key_alignment.py`）

```
$ python scripts/ops/verify.py --batch guards --quiet
✓ [guards] tests/guards/（门禁退出码契约 + 验证规范）  exit=0  21.83s/300s  exit=0
（同一批在三个时点的读数：开工基线 26.04s / 首次交付基线 37.06s / 二次合并后 21.83s
 —— 与 `main` 新立的**口径 21**（门禁口径自身有 ≈2.9× 波动）一致，**不宜拿单次读数下结论**）
EXIT=0
```

### 2.5 全门禁

```
$ sh system/scripts/ops/pre-commit.sh
门禁条数 = 14 ; RESULT: PASS 行数 = 14
pre-commit → append_only_guard / rules_lock_guard / registry_schema_guard / schema_sync_guard /
             conflict_scan(L1-L5) / no_placeholder_guard / injection_guard / verification_policy_guard /
             shell_var_guard / graph_integrity_guard / criterion_effectiveness_guard /
             quote_provenance_guard / scenario_tag_binding_guard / rule_key_alignment_guard
pre-commit ✓ 全部门禁放行
RC=0
```

`verification_policy_guard` 的读数（新守卫文件是否被批次覆盖，就看 `test_files_uncovered`）：

```
== verification_policy_guard.py ==
  scanned batches: 23            ← 与改前一致（本卡未增删批次）
  scanned max_timeout_s: 300     ← 与改前一致
  scanned test_files: 78         ← 改前 76（+1 = 本卡新守卫，+1 = main 新增的 rule_key_alignment 测试）
  scanned test_files_uncovered: 0
RESULT: PASS（0 violations）
```

### 2.6 `V-06` / 分片配额：本卡**零影响**（实测）

```
$ python /tmp/n13/measure.py       # 逐片 --collect-only，零夹具
INJECTION_SHARDS 长度 = 7
逐片：a=30  b=32  c=31  d=29  e=32  f=33  g=18
逐片求和 = 205 ；整目录 --collect-only = 205（两口径一致）
最大片 = injection-f 33 ⇒ 余量 -1
```

本卡**没有**向任何 injection 片加入测试文件 ⇒ 上表在**开工基线 `ed9687b` 与交付基线 `56fd637` 上都测得同一组数**。

---

## 三、每条 DoD 的证据

| DoD | 要求 | 证据 | 结论 |
|---|---|---|---|
| ① | 全文搜 `6 片` / `六片` / `injection-f` 枚举上界 / 旧分片数 ⇒ **逐处处置并贴原文** | §一 的 21 处逐处表（每处都贴了**改前逐字原文**与处置）＋ **§五.1 的 2 处**（`injection-a..f` / `a..f`，首轮漏网）；§二.1 与 §五.3 的改前/改后清点输出。**我的实测计数是 11 处片数/轮次数字写法**（`:43 :72 :91 :94 :226 :228 :237(×2) :238 :313 :516 :518`）+ **1 处枚举上界**（`:29`，另有 §五 的 2 处范围式）+ **2 处旧分片数**（`:362 :383`）；另**顺带**发现并处置了同类漂移 **8 处**（`:227 :327 :347 :359 :362 :383 :494 :505`）与 **1 处活的自相矛盾**（`:255` + `:270-274` vs 参数 `120.0`） | ✅ 全部处置（**含首轮漏掉的 2 处**）；★ 与任务书"**8 处散文仍写 6 片**"的计数**不一致**（我数出 11 处），差 3 处 —— 见 §四 缺口 1，**且 §五.5 指出"数 8 处"这种数法本身就抓不到 `a..f`** |
| ② | 新守卫或断言（**能派生的话**） | §一.4 的三条判据 + **§五.2 的第 ④ 条**（**片名集合 / 片字母集 / 范围两端全部由 `INJECTION_SHARDS` 现算**，无一处写死片名或片数）；`11 passed` + **6 条活体反向对照全部成立**（含中文「六片」、③ 的 `a27…`、④ 的两种范围写法）| ✅ 已交付，且**非真空**（基线 + 反向 + 反向的反向三类用例齐备；④ 的 bug 就是被"反向的反向"抓出来的） |
| ③ | 报告贴**改前改后 `grep`** 结果 | §二.1 两份同脚本输出（改前 70 处命中 → 改后 ①/③ 归零、② 只剩真源与单个交叉引用、④ 只剩 ⑥ 类实测读数） | ✅ |
| ④ | **不要动 b/e 两片的内容**（余量 0） | ① `git diff` 只含 `scripts/ops/verify.py` 与新增 `tests/guards/test_verify_prose_binding.py`，**未触碰任何 `tests/injection/**` 文件**；② 新守卫落在 `tests/guards/`，`injection-f` 的收集数（33）在改前改后**完全一致**（§二.6）；③ 两个片的文件清单（`BATCHES` 字面量）逐字未动 | ✅ |

处置纪律（13-F 先例）：
- **能删的删掉**（21 处里绝大多数是直接删）；
- **删不掉的从真源派生**（`片数 = INJECTION_SHARDS 的长度`、`真值见 Batch.timeout`、`真源 = MAX_CASES_PER_SHARD`，全部给出**查真值命令**）；
- **不许在散文里再抄一份计数** —— 包括我**自己新写的那段声明**：初稿我写了「同一处漂移在本文件里出现 **8 次**」，随即**自己删掉**（引用了任务书里的计数 = 又抄一份），改为「成片复发，逐处清单见本报告」。

---

## 四、剩余不确定性与缺口

### 4.1 ★ 阻塞：`main` 上 `injection-f` **还有一条红**（**不源自本卡，且我不擅自修**）

| 红 | 状态 | 实测 / 根因 |
|---|---|---|
| ① 实现丢失 | **已被 `main` 修掉**（我报出后 `main` 前进的 22 个提交里补回了实现） | 我开工时在 `ed9687b` 实测 `1 failed`：`AssertionError: 归因文案应同时指明是「环境」问题与「pytest」缺失，实际是: 'exit=1'`。根因：`ea0ceae`（"merge ws/verify-shard（口径 11 补合）—— **冲突取主干侧**"）对 `verify.py` **取主干侧**，丢掉了 `90e58b0` 新增的 `PYTEST_MISSING_MARKER` 常量 + `_exit_zero` 的环境归因分支；而同一合并对 `test_shard_coverage.py` **取分支侧**，把断言留下 ⇒ 「**测试有了、实现没了**」。**交付基线 `56fd637` 复测：该条已 PASS。** |
| ② 片配额越限 | **仍红**（交付基线 `56fd637` 复测仍是它） | `test_shard_case_counts_within_quota`：`AssertionError: 以下片的用例数超过上限 32: {'injection-f': 33}（现算值）`。根因：同一个补合并把 `test_shard_coverage.py` 从 4 例带到 6 例，而它**自己就住在 f 里** ⇒ f: 31 → 33 |

```
$ python -m pytest tests/injection/test_shard_coverage.py -q -p no:cacheprovider
# 开工基线 ed9687b：2 failed, 4 passed
# 交付基线 56fd637：1 failed, 5 passed   ← 只剩红②
FAILED tests/injection/test_shard_coverage.py::test_shard_case_counts_within_quota
```

**我为什么不修红②**：修法需要**移出一个文件**（例如 `test_shard_coverage.py` f→g：f 33−6=27、g 18+6=24），
而 `test_shard_coverage.py:110` **逐字**写着「**片数是主理人决策**，本文件不擅自改」。
已上报 team-lead 请求裁决（**未擅动**）。
★ 这也是本卡把新守卫放 `tests/guards/` 的直接原因 —— 落在 `tests/injection/` 会把 f 抬到 34。

### 4.2 缺口 1（**计数不一致，需裁决**）：任务书说"8 处"，我实测 **11 处**

任务书写「同文件 **8 处**散文仍写"6 片"」。我用同一脚本清点得到 **11 处**带片数/轮次数字的行
（`:43 :72 :91 :94 :226 :228 :237 :238 :313 :516 :518`，其中 `:91/:228/:237` 各含 2 个命中）。
差异可能是"按处 vs 按行"或"是否把 `≈14 片` 计入"。**我按"逐行逐命中"报，以脚本输出为凭**，
不猜任务书的数法 —— 处置上**没有区别**（11 处全部处置）。

### 4.3 缺口 2：超时值复述**未逐个删除**，只做了"降格 + 声明"

`BATCHES` 里各批注释仍以"倍数/沿革"形式复述自身超时值（`guards` 的 `60s → 300s`、
`pricelayer` 的 `取 300s（≈3.6~5.6×）` 等）。**逐个删会掏空 `V-02` 的取值理由**（倍数离开数值就不成立），
故只做了两件事：① 修掉**与真源矛盾**的那一处（`unit`：散文 270s vs 参数 120.0）；
② 在字典前新增声明段，把这类数字**统一降格为"取值沿革/留档"**并声明真源 = `Batch.timeout`。
⇒ **建议把"超时值复述"登记为新缺口（可判定形态：注释里的秒数与参数不等即违规）**，
是否逐个改由主理人裁 —— 本卡**只报了不擅改**。

★★ **本卡交付期间，这条缺口当场复发了一次**（见 §一.5）：`main` 把 `unit 120→300`、
`injection-g 90→300`，我散文里的**三条理由**立刻过期。⇒ 这条缺口的**严重性已被实证**：
它不是"风格问题"，而是**下一次改超时值就必然产生的假信息**。
★ 同时它也给出了一条**低成本中间态**（本卡采用的）：**把"沿革"与"当前值的理由"物理分开** ——
沿革写在批注释里（含历史值，声明为留档），**当前值的理由只写在该批参数之后的注释里**，
并在 `BATCHES` 前声明"真源 = 位置参数"。这样值一变，**过期的只可能是"沿革"，
而沿革本来就声明为历史**，不会变成假信息。

### 4.4 缺口 3：守卫的**表达空间**非穷尽（诚实登记）

判据 ②/③/④ 是**正则**，只能覆盖**已见过的写法**。未覆盖：英文（`six shards`）、罗马数字（`Ⅵ`）、
`三点五片` 这类非常规表达。**这不构成"门禁被静默关掉"**（判据覆盖的是本文件已真实出现的全部写法，
且 6 条反向对照逐条证明其非空），但**不能声称"穷尽于自然语言"**。
⇒ 建议登记 `T` 号；若主理人要求更强，可改为"**正向白名单**：散文里出现任何数字+单位组合都必须显式声明真源"，
但那是**另一个尺寸的改动**，不在本卡。

★★ **这条缺口已被实证过一次**：首轮交付时 ①（"同一处 ≥2 个全名"）**漏掉了** `injection-a..f`
——因为它只含 **1 个**全名（右端是裸字母 `f`）。而这正是本卡的题面本身。
⇒ 收尾时补了 ④（范围式枚举 + 上界过期），见 §五。**"判据覆盖了已见写法"这句话本身也会过期。**

### 4.5 缺口 4：`test_shard_coverage.py` 里有**同族漂移**（不在本卡范围）

该文件（不在本卡对象 `verify.py` 内）的散文同样带着会漂移的量：
`:5` 「165 个用例」、`:6` 「273 项」、`:113` 「6 片立刻不成立（目录现有 **171 例**）」、
`:111` 「`a` 27 例 / `b` 28 例 / `d` **31 例** / `f` 31 例」、`:104/:331` 「需 ≈14 片 / 14 个轮次」。
**现值**为 `a=30 b=32 c=31 d=29 e=32 f=33 g=18`（合计 205）⇒ **`:111` 与 `:113` 已过期**。
不擅动：① 它是 `ws/verify-shard` 的交付物，且**当前红着**（§4.1），在红文件上叠加改动会让归因变脏；
② 它自己的 `MAX_CASES_PER_SHARD` 注释明确"改片数要主理人定"。**建议单独开卡**（口径 15：补合并残留应按"事实集合"粒度复核，而不是按文件）。

### 4.6 环境性说明（非本卡缺陷）

- 逐片/整目录的 `--collect-only` 一律带 `--noconftest`（防外层会话的 `tests/.work/` 被删，`V-05` 姊妹问题）；
- 全部验证子进程在 broker hook 关闭状态下跑（`V-04`）；本卡新增守卫**不用 `code_root`**，故**不消耗宿主删除配额**；
- 项目解释器：`$HOME/.workbuddy/binaries/python/envs/default/bin/python`（`python3 -m pytest` 会报 `No module named pytest`，那是解释器问题、不是测试坏了）。

### 4.7 ★ 观察（建议升格为口径）：「把数字改对」才是这类漂移真正的危险形态

本次交付期间 `main` 前进了 22 个提交，其中一次**已经"修"过**本卡的题眼 —— 把表里那行从
「`injection-a`…`injection-f` + **6 个分片**」改成「`injection-a`…`injection-g` + **7 个分片**」。
**它看起来是修好了，实际是把"漂移计时器"重设了一次**：下一次开 `injection-h`、或把某片再拆一片，
同一处会**原样再错一次**，而且因为"上次是对的字面"，review 时更不容易被注意到。

⇒ 这条正是本卡三条判据**必须由真源现算、且必须"删"而不是"改对"**的原因；
也是 `G-28` 的四次复发里，**第一次有了机器绑定**（此前四次都只靠"下次注意"）。

### 4.8 复核入口（给独立审计）

```bash
# 1) 两条判据是否仍为零违规（含基线）
python -m pytest tests/guards/test_verify_prose_binding.py -q
# 2) 反向对照（逐条注入 ⇒ 必须红；自动还原并校验 sha256）
python /tmp/n13/reverse.py
# 3) 改前/改后清点（同一脚本，可 diff）
python /tmp/n13/inventory.py system/scripts/ops/verify.py
# 4) 真源读数（片名单 / 超时 / 每片用例数）
python -c "from scripts.ops.verify import INJECTION_SHARDS, BATCHES; print(len(INJECTION_SHARDS), INJECTION_SHARDS); print({n: b.timeout for n, b in BATCHES.items()})"
python /tmp/n13/measure.py
```

★ 三条外部脚本都放在 `/tmp/n13/`（**未入库**）：它们是一次性证据工具，不是交付物；
入库的只有 `verify.py` 的散文改动与 `tests/guards/test_verify_prose_binding.py`（守卫本身）。

---

## 五、收尾补记：**首轮漏了 2 处**（`injection-a..f` / `a..f`），并为此补第 ④ 条判据

**起因**：首轮交付后我按 DoD ① 的要求再全文搜一遍「枚举上界」，用**当前尖端**（`052cb5f`）重跑清点时，
`②枚举上界` 类里出现了两行 I 没处置过的写法 —— 它们在 `injection-g` 批的注释里，
**是第二次合并（`main` 的 `96d4d7e`）带进来的**，而我在 `5fe0879` 那一轮复核时只改了同一段里
那个 `90s`（"理由漂移"），**没看见同一段的枚举上界**。

### 5.1 逐处原文（改前 = `052cb5f:system/scripts/ops/verify.py`）

| # | 改前逐字原文 | 为什么是退化 | 改后 |
|---|---|---|---|
| 25 | `# ★ 超时 90s → **300s**（主理人裁定）：与 `injection-a..f` **齐平**（同族一致性优先），` | 范围式枚举，**上界停在 `f`** 而真源已到 `g`；且 **① 判据抓不到它**（该行只含 1 个全名 `injection-a`，右端是裸字母） | `# ★ 超时（主理人裁定）：与**其余各片**齐平（同族一致性优先；片名单不在散文里枚举，` ＋ 下一行 `#   真源 = `INJECTION_SHARDS`）。**当前真值见下一行的 `Batch.timeout`**，本段只留沿革。` |
| 26 | `#   会被判 `[TIMEOUT]`，而同期 `a..f`（300s）不会。⇒ 只是让本片**不比邻片更早红**。` | 同上（且这里**连前缀都没有**，纯裸字母范围） | `#   （远超旧值 90s）会被判 `[TIMEOUT]`，而同期其余各片不会。⇒ 只是让本片**不比邻片更早红**。` |

★ 处置口径与首轮一致：**删枚举**（不改成 `a..g` —— 改成对的上界同样是"第二存放处"，下一次开 `h` 再错一次，见 §4.7）。
★ 顺手把同段的 `**247s ≫ 90s**` 拆开写为 `**247s**（远超旧值 90s）`：`90s` 是**沿革**，
不该以"对比当前值"的姿态出现（§4.3 的中间态口径）。

### 5.2 新增第 ④ 条判据（`test_verify_prose_binding.py`）

| # | 退化 | 判据 |
|---|---|---|
| ④ | 散文里的**范围式枚举上界已过期**（`injection-a..f` / `a..f`） | 两端字母由 `INJECTION_SHARDS` 的后缀集现算，**右端 ≠ 最大后缀**即违规 |

- **刻意放行**右端 = 最大后缀的范围（`a..g`）：它不与真源冲突。判据只抓"常量加了片、散文没跟上"。
  若连正确范围也判红，正当注释会被逼到无法书写 ⇒ 门禁被关掉（`G-01`）。
- 这条是**从真源派生**的：坏样本在测试里**现拼**（`suffixes[-2]`），片名一变，注入样本自动仍为"过期上界"，
  反向对照不会跟着腐坏。

### 5.3 证据（真实输出 + 退出码）

```
$ python /tmp/n13/inventory.py …            # 改前 = 052cb5f:verify.py
===== ①片数/轮次  （4 行）=====      ⇒ 全是惯用语「一片」
===== ②枚举上界  （29 行）=====      ⇒ 其中 1 行是本次的缺陷（:456 的 injection-a..f）
===== ③旧分片数  （0 行）=====
===== ④用例数/上限  （20 行）=====  ⇒ 全是 ⑥ 类实测读数
合计命中行数：53

$ python /tmp/n13/inventory.py …            # 改后 = 工作树
===== ①片数/轮次  （4 行）=====
===== ②枚举上界  （28 行）=====      ⇒ 缺陷行消失（29 → 28）
===== ③旧分片数  （0 行）=====
===== ④用例数/上限  （20 行）=====
合计命中行数：52

$ python -m pytest tests/guards/test_verify_prose_binding.py -q -p no:cacheprovider
...........                                                               [100%]
11 passed in 0.51s          EXIT=0        ← 首轮是 10 passed（多的一条即 ④ 的反向对照）

$ python /tmp/n13/reverse.py
基线 sha256(verify.py) = a5aa09676a86d6ca…
基线（未注入）守卫：exit=0  1 passed, 10 deselected in 0.51s
✓ 注入 ①枚举上界          ⇒ 守卫 exit=1（红=True）判据号=①（期望 ①）
✓ 注入 ②片数（阿拉伯）      ⇒ 守卫 exit=1（红=True）判据号=②（期望 ②）
✓ 注入 ②片数（中文「六片」）  ⇒ 守卫 exit=1（红=True）判据号=②（期望 ②）
✓ 注入 ③旧分片数          ⇒ 守卫 exit=1（红=True）判据号=③（期望 ③）
✓ 注入 ④范围枚举（带前缀）    ⇒ 守卫 exit=1（红=True）判据号=④（期望 ④）
✓ 注入 ④范围枚举（裸字母）    ⇒ 守卫 exit=1（红=True）判据号=④（期望 ④）

还原后 sha256(verify.py) = a5aa09676a86d6ca…  ✓ 与基线一致
反向对照结论：全部成立           EXIT=0
```

★ 补第 ④ 条时，**我自己的"反向的反向"用例当场抓到了第 ④ 条的一个 bug**：
`_split_range` 用 `re.split("\.\.|…|–|—|-")` 拆范围，而 `injection-a..g` 里**有连字符**，
于是左端被拆成 `injection` ⇒ **把正确的 `a..g` 误判为"上界停在 a..g"**。
改为**用正则捕获组取两端**，并把这条 bug 写进该函数的 docstring（防止后人再改回 split）。
⇒ 这就是"必须写反向的反向用例"的实证：没有那一条，守卫会以"过宽"的形态上线。

### 5.4 仍然"只动散文"（机器可核，最终尖端）

```
$ python /tmp/n13/tokproof2.py          # 对象 = main:verify.py  vs  工作树
剥离所有注释 + 所有 docstring 后：AST-unparse 文本相同 = True
该等价形态下的代码 token 数：main = 1867   HEAD = 1867   逐一相同 = True          EXIT=0
```

首轮报告的 `1895 / 1895` 是**改前**（含 docstring 正文）的数法；本处改为
**剥离所有注释与 docstring 后再比 token 流**，比首轮那个更严（连 docstring 正文都不许影响结论）。

### 5.5 收尾补记对首轮结论的修正

| 首轮结论 | 现修正 |
|---|---|
| "共 21 处" | **23 处**（+ 上表 25/26）。★ 我**不**去改首轮那句 —— 它标了"行号以 `ed9687b` 为准"，语义仍然是"首轮那一批" |
| 守卫"三条判据" / `10 passed` / "4 条反向对照" | **四条判据 / `11 passed`（含 1 条 ④ 的反向对照）/ 6 条活体反向对照** |
| §4.4 "判据覆盖本文件已真实出现的全部写法" | **这句话本身也会过期** —— 首轮它就漏了 `injection-a..f`。已在该节写明 |
| 与任务书"8 处"的计数差（§4.2） | 不变（那是首轮口径问题）；但说明**"数 8 处"这个数法本身就是这次漏网的原因之一**：按"6 片"字样数，**永远数不到 `a..f` 这种写法** |
