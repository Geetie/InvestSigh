# 批次 8 任务书 —— 并行机会清单与已派出流

> **签发人**：齐活林（交付总监 / 主理人）· **日期**：2026-09-16
> **对拍基准**：`00_交付施工图 §5 关键路径与并行机会` · `00_开发Agent开工提示词 §5.1/§九` · `system/CONVENTIONS.md`
> **纪律**：本任务书先于代码产出。未写出的标准 = 未满足的标准。

---

## §0 先说一条与"并行"直接相关的**负面证据**

批次 5 我们同时开了四条流（`ws/compute` · `ws/graph` · `ws/claim` · `ws/decision`）。
§九 独立审计的结论是：**四条流的 `AC-03 真接线` 全部不合格**（FAIL 5 / PARTIAL 5），
根因同构 —— `pipeline.py` 一个 handler 都没注册，**四条流全是孤儿模块，从未被主流程调用**。

**共同教训**：本项目此前几乎所有缺陷都源于**"从没被真实输入跑过"**
（阻断级 `N-2`、四条静默缺陷、`P7-1` 空仓库伪造产物 —— 无一例外）。

→ **因此本批次不再"能并就并"，而是给每条流一个硬前提：它必须能被真跑，且必须有人调用它。**

---

## §1 已派出的两条流（可立即开工，互不冲突）

### 1.1 `ws/real-collect` —— 阶段② 最小真实采集（**关键路径第一步**）

| 项 | 内容 |
|---|---|
| 工作树 / 分支 | `.worktrees/ws-real-collect` ／ `ws/real-collect`（基点 `main` = `d03418a`） |
| 文件面 | 新增 `system/raw/**` · 追加 `system/facts/*.jsonl` 行 · 必要时 `scripts/ingest/**` · 报告 |
| 目标 | 采 NVIDIA 官方**一级**材料 → 走 step 1 落**真实** `claims` → **用真实数据跑一次 `superseded` 传播** |
| 为什么它最优先 | ① 它是阶段② 的第一步；② 它是**唯一能根治"空真源/孤儿"这一整类问题**的动作；③ 它顺带完成 `N-2` 修复的**真实数据验收** |

**四条硬约束**：只采公开来源 · **一级优先**且二三级只作线索 · `raw/` 原文**不得改写** ·
**核不到就写"核不到"，不许编**（`§一 底线 3`）。

### 1.2 `ws/ch7-transmit` —— `Ch7 §C.1` 传导编排（阶段③ 承重块）

| 项 | 内容 |
|---|---|
| 工作树 / 分支 | `.worktrees/ws-ch7-transmit` ／ `ws/ch7-transmit`（基点 `main` = `d03418a`） |
| 文件面 | 新增 `system/scripts/transmit/**` + `system/tests/transmit/**` + 两份报告 |
| 依据 | `Ch7 §C.1` 明确要求 `engine.py` / `demand.py`；且 `ws_graph_dod.md` 的「**不做**」清单**逐字**把 `scripts/transmit/` 留给本流（graph 只提供原语） |
| 强制 | **先写 DoD 再写代码**（`§5.1`）· **必须复用** `scripts/graph/**`（`G-06`）· 参数不得内置阈值 · 禁跳级等同实测真抛 |

---

## §2 ★ 现在**不能**开的流，以及为什么（这比"能开什么"更重要）

| 流 | 设计里有吗 | 为什么现在不开 |
|---|---|---|
| **Ch5 共识层** | ❌ **设计区里没有** | 外部同题作业实测"共识层三包零覆盖"，其口径与框架（评级分布时间序列 / 一致预期与修正 / 共识饱和度三层次）**是外部方法、不是本项目设计**。按 `R-04`「设计未写的不新增」，**必须先由需求方裁定 `S-1`**，否则就是**自造需求**。 |
| **Ch4 价值层** | ✅ 有 | 其模型侧（护城河判定 / 增长驱动）**没有真实数据就无从真跑** → 会重演"孤儿"。**触发条件：`ws/real-collect` 的真数据进 `main` 之后。** |
| **Ch6 证据层** | ✅ 有 | 同上。且其确定性部分（定位机械复查）**已由 step 2 接上**，下一步是模型侧（`Ch6 §6.3` 七步判定），同样需要真数据。 |
| **守卫收口（`G-28` 契约矩阵等）** | ✅ 有 | 涉及 `tests/guards/**` 与 `pipeline.py`，而 **`I-1`/`chain_steps.py` 正在被审计**。改它会**让审计结论失效**。→ 等 A 单审计结束，由主理人自己做。 |

**一句话**：**剩下的并行机会都存在，但都有前置条件；现在强开只会再造一批孤儿。**

---

## §3 并行机会总表（含触发条件，供后续排期）

> 依据 `00_交付施工图 §5.2`：阶段② 内 `Ch4 价值层 ∥ Ch5 价格层`；阶段③ 内 `Ch6 证据层 ∥ Ch7 传导层`。
> 两组的共同前提都是「**接口先冻结**」，本项目的接口载体 = `system/schema/models.py`（已有）+ 各流 DoD。

| # | 流 | 文件面（互斥） | 前置条件 | 状态 |
|---|---|---|---|---|
| **F1** | `ws/real-collect` | `raw/**` · `facts/*.jsonl` · `ingest/**` | — | 🔄 **已派出** |
| **F2** | `ws/ch7-transmit` | `scripts/transmit/**` | — | 🔄 **已派出** |
| **F3** | `ws/ch4-value` | `scripts/value/**` | F1 真数据进 `main` | ⏳ 待触发 |
| **F4** | `ws/ch6-evidence` | `scripts/evidence/**` | F1 真数据进 `main` | ⏳ 待触发 |
| **F5** | `ws/ch5-price` | `scripts/price/**` | **需求方裁定 `S-1`** + F1 | ⛔ 被设计前提阻塞 |
| **F6** | `ws/guards-catchup` | `tests/guards/**` · `pipeline.py` | A 单审计结束 | ⏳ 待触发（**主理人自己做**） |
| **F7** | `ws/decision-fix` | `scripts/decision/**` | — | 🔄 **已派出**（`P7-1` 阻断级修复） |

---

## §4 铁律（对所有参与方，逐条硬约束）

- **W-1** 只在自己的工作树里工作（绝对路径）；**W-2** 绝不 `cd` 进主仓库或他人工作树；
  **W-3** 绝不切分支（`checkout`/`switch`/`reset`/`rebase`/`stash`/`merge`/`worktree` 全禁）。
- **G-2** 只 `git add <具体文件>`，**永不** `-A`/`.`；**G-3** 只在自己分支提交，**不做集成**；
  **G-4** 绝不 `gc`/`prune`/`repack`/`clean`；**G-5** 提交信息用 `-F <文件>`；
  **G-7** 不与其他成员直连，只回主理人。
- **V-01~V-06**：禁全量验证 · 每批独立超时 · **超时=不合格** · **沙箱外跑** · 串行留档 · 新测试目录须报"需新增批次"。
- **R-06** 判据必须**可判定 + 穷尽**；禁关键词/名单式判据；允许清单优于禁止清单。
- ★ **新建工作树第一步**：`sh system/scripts/ops/bootstrap_worktree.sh`（`V-07`）。
- ★ **新建测试目录** → `V-06` 必红是**预期**的；`verify.py` 是共享文件，**由主理人统一加批次**，
  不许自行改；若为唯一红门，可按 `§1.5` 三条件用 `--no-verify` 并**显式披露**。
