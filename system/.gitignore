# system/.gitignore —— 只忽略**可再生 / 运行产物**，绝不忽略真源。
#
# ★ 为什么放在 `system/` 内而不是仓库根：`00_开发Agent开工提示词 §三` 规定
#   **设计区只读**（仓库根的 `00_*` 与 `01_`~`11_` 不改）。把忽略规则收在
#   代码区自己的目录里，既不改仓库根，又能精确约束代码区的产物。

# ── 测试夹具的临时副本（**绝不能进仓库**：那是整个 system/ 的拷贝）──
tests/.work/

# ── Python 缓存 ──
__pycache__/
*.py[cod]
.pytest_cache/
.venv/

# ── 可重建索引（`Ch9 §3.3.2`：索引非真源，删掉可从 facts/ 全量重建）──
index/
!index/.gitkeep

# ── 检查器运行产物（命中时写的报告；`Ch2 §B.4`）──
reports/*.json
reports/*.log

# ── 文件锁（运行时态，非真源）──
*.lock

# ── 阶段① 通过判据的证据留存（`00_开发Agent开工提示词 §十` 进度落盘）保留 ──
!PROGRESS.md
