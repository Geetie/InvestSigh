# AI 产业链研究 Agent · 交付施工图

> **文件性质**：本文件是**新增件**（不在源稿 11 章体系内），服务于"**收口完成 → 交付实现**"的过渡。它**不新增任何需求、不新增任何参数、不新增任何门槛**——只把 11 章的成果**重新编排成一条可施工的路径**。
> **本文件日期**：2026-09-15 ｜ **版本**：v1.1（2026-09-15 回改）。**本文件回改明细见文末「附：回改记录」（唯一权威）** ｜ **对齐**：`11_交付顺序与实施参数/02_实现方案.md`（交付主线权威）+ `09_数据与实现约束/09_需求拆解与实现方案_v1.md §3.9`（实现细化挂靠）
> **引用规范**：全文跨文件引用一律用**节号锚点**（`Ch9 §3.3.3`、`Ch11 §B`），**不使用绝对行号**。
> **全包口径**：11 章 **376 条 = ✅327 / ⚠️49 / ❌0**；11 项待冻结参数 = **6 A（需需求方拍板）+ 5 C（实现方给方案）**。

---

## 0. 这份图怎么用（五条纪律，先读）

1. **主从关系不许反**——施工主线 = **源稿五阶段**（`11/02 §B`，权威）；`09 §3.9` 只作**每阶段的实现细化挂靠**。两者名称/数量/顺序逐字一致，**不合并、不并列**（承 `11/01 §2.1` 裁定 C11-1）。
2. **"触及 ≠ 才实现"**——某阶段"涉及"的需求/组件，**不等于**"该阶段才实现"。P0 需求**跨阶段**，**不得据阶段表反向裁剪需求**（`11/02 §附`）。
3. **阶段未过即阻塞**——每阶段通过判据未过 → **不出该阶段交付物，不得静默跳过**（`11/02 §B`，落 `scripts/delivery/stage_gate.py`）。
4. **本图不替设计做决定**——各组件的设计细节以各章 `02_实现方案.md` 为准；本图只给"**顺序 / 归位 / 依赖 / 口径 / 验收点**"。
5. **参数未冻结不阻塞开工**——11 项参数以 `tbd` 占位 + 可切换即可开工；**A 档拍板是"验收前置"，不是"开工前置"**（除阶段②需 p05 样例方法、阶段④需 p07 排期——属"该阶段触及"，**非硬门**，承 `11/02 §F.2`）。

---

## 1. 施工总览

### 1.1 五阶段主线（`11/02 §B`）

```
① prep ──▶ ② nvidia_sample ──▶ ③ core_chain ──▶ ④ daily_run ──▶ ⑤ expansion
需求与来源准备    NVIDIA 深度样例      核心产业链运行     每日持续研究      覆盖扩展与评估

每阶段：通过判据未过 → 阶段阻塞（不得静默跳过）
```

### 1.2 阶段 → 交付物 → 涉及章节 → 组件归位

> **归位原则**：按"该阶段要产出什么"倒推需要哪些组件；**同组件只归到它首次被需要的阶段**，后续阶段复用不重复列。

| 阶段 | 交付物（源稿逐字） | 通过判据（可测） | 主要涉及章节 | 新增组件数 |
|---|---|---|---|---|
| **① `prep`** 需求与来源准备 | 本文、首批关系核验清单、公开数据登记、基准及关键口径 | 四类齐备；"已确认规则 vs 建议实现方式"**有显式区分**；口径字段 `tbd` 占位 | **Ch9**（地基）· **Ch2**（规则与检查器）· **Ch3**（范围与基准）· **Ch1**（编排骨架）· **Ch11**（参数与交付配置） | ~36 |
| **② `nvidia_sample`** NVIDIA 深度样例 | 完整经营/竞争/财务/估值/基准比较与建议底稿 | **六步判断链完整**（Ch1 五问 + Ch10 六结果）+ 证据可定位 + 推导可复查；满足 Ch4 §G | **Ch4**（公司价值）· **Ch5**（价格与预期）· **Ch9**（compute 层） | ~23 |
| **③ `core_chain`** 核心产业链运行 | 上/下游与竞争者独立底稿 + 真实事件联动 + 图谱与问题任务 | 多公司传导跑通（Ch7 §C.7）+ **T01–T14 全过**（`10/01 §2.1`）+ 图谱/提问任务可追溯 | **Ch6**（公开信息与证据）· **Ch7**（传导与建议）· **Ch9**（graph 层） | ~26 |
| **④ `daily_run`** 每日持续研究 | 固定更新 + 事件触发 + 增量发布 + 故障降级与建议记录 | 连续运行下**时点可核**（Ch9 §2.2）+ **覆盖可核** + **任务状态可核**（Ch8 §C.2）；降级**保留上次有效结果** | **Ch8**（入口与每日运行）· **Ch1**（Automation 编排）· **Ch9**（ops 层） | ~19 |
| **⑤ `expansion`** 覆盖扩展与评估 | 逐步补齐其他公司 + 校准公开信息筛选 + 持续前向复盘 | 研究标准**保持一致**（Ch4 §G）+ 投资结果**可核验**（Ch10 §N10.3-04）；复盘 **append-only** | **Ch10**（验收与复盘）· **Ch9**（快照与权限） | ~13 |
| **横切**（贯穿全阶段） | —— | —— | Ch1 编排器 · Ch2 检查器（4 时机） · Ch11 守卫 | 已在各阶段计入 |

### 1.3 阶段 × WorkBuddy 复用口径

> 口径定义（全包统一）：**A = 开箱即用零代码 / B = 写 Skill·配置·提示词即可 / C = 需独立代码**。判定原则：**能 A 不 B，能 B 不 C；C 档只保留"可单测断言"的薄层**。

| 阶段 | A（零代码，直接复用） | B（Skill / 配置 / 提示词） | C（需独立代码，承重） |
|---|---|---|---|
| ① | MCP 连接器取数（westock / iFinD / pandadata）· 网页采集（WebSearch / WebFetch）· 文件系统持久化 | 目录与 schema 定义（pydantic / JSON Schema）· `rules/*` 只读配置 · 来源登记 Skill · Git 版本化 | 检查器 4 件（Ch2）+ 3 件（Ch3/Ch11）· `return_guard` · `stage_gate` |
| ② | SubAgent 并行研究 · 文件系统 | 研究底稿生成 Skill · 指标集配置 · 假设生成 | **反向求解器 · 估值路由 · 倒填/顺序守卫 · 护城河判定 · 增长质量**（C 档最密集的阶段） |
| ③ | SubAgent 并行 · Automation 事件触发 · 复用 Ch6 `claims` | 采集编排 Skill · 传导编排 Skill · 建议展示 | **三维决策函数（序/信/深）· 去重与传播 · 传导引擎 · 环检测 SCC · 建议决策引擎** |
| ④ | **Automation（`rrule`）** · SubAgent 任务队列 · present_files 三入口 | `aichain-daily`/`aichain-ask` Skill · `rules/trigger|schedule|notification|publish.yaml` · 视图 | **降级写路径守卫 · 幂等与增量不变量 · 展示层中立守卫扩项** |
| ⑤ | present_files 展示 · 腾讯文档分享 | 三层复盘记录 Skill · 复盘视图 | **`error_axis_guard` · 复盘收益过 `return_guard` · 六结果追溯与三层不合并不变量 · 前向能力来源断言** |

**一句话结论**：**交付编排、登记、展示、取数、并行调度全部 A/B 档直接复用 WorkBuddy**；必须自建的（C 档）**只有"可单测的确定性断言"那几块**——集中在**估值/传导/回测**三处算法，与**守卫/检查器**一族。

---

## 2. 逐阶段施工清单

### 阶段① `prep` 需求与来源准备（地基）

> **进入条件**：整包文档确认 ｜ **通过判据**：四类交付物齐备 + "已确认规则 vs 建议实现方式"显式区分 + 口径字段 `tbd` 占位 ｜ **退出物**：`registry/sources.yaml` + 口径表（`freeze_status=tbd`）

**要建什么**：

| 类型 | 名称 / 落位（包内相对） | 作用 | 出处 | 口径 |
|---|---|---|---|---|
| 骨架 | `/Users/gaza/Developer/InvestSigh/`（Git 仓库根：`rules/` `registry/` `raw/` `facts/` `derived/` `index/` `snapshots/` `views/` `scripts/` `skills/` `tests/`） | 8 类对象模型与目录地基 | Ch9 §3.3.3 | B |
| 数据 | `facts/` **18 个追加式 JSONL**（8 类对象唯一真源） | 事实源 | Ch9 §3.3.3 | B |
| 配置 | `rules/`（只读 0444 + SHA256 锁，模型不可写） | 规则冻结层 | Ch9 §3.4.11 | B |
| 登记 | `registry/`（来源登记表 7 字段 + 公司行动表 + 人工质量标签） | 来源与行动登记 | Ch9 §N9.3 | B |
| 数据 | `raw/`（页面快照 / PDF / 字幕 / 合法保留片段） | 原始物留存 | Ch9 §N9.1-10 | B |
| 配置 | `rules/banned_tokens.yaml`（token 级 `scope`） | **禁词表**（全包唯一禁词源） | Ch2 §B.2 | B |
| 检查器 | `scripts/checks/conflict_scan.py` | L1–L5 聚合入口（AST + 禁词 + 配置扫描） | Ch2 §B.3 | C |
| 检查器 | `scripts/checks/assert_gate_input.py` | 决策门输入结构断言（P-06） | Ch2 §B.3 | C |
| 检查器 | `scripts/views/neutrality_check.py` | **展示层中立守卫**（P-10，与 Ch3 §D.1 共用） | Ch2 §B.3 | C |
| 检查器 | `scripts/checks/module_denylist.py` | 模块 denylist（禁 gold/btc/crypto/跨资产/组合优化/账户/交易执行） | Ch3 §D.3 | C |
| 脚本 | `scripts/benchmark/return_guard.py` | **基准收益守卫**（禁替代指数 / 禁二次扣费 / 净值仅诊断） | Ch3 §N3.4-05/07 | C |
| 脚本 | `scripts/scope/anti_padding.py` | 反凑数三件套（无目标数量 + 入选三字段非空） | Ch3 §N3.1 | C |
| 配置 | `rules/benchmark.yaml` · `rules/scope.yaml` | 基准对象声明 / 推荐范围声明 | Ch3 §B-17 | B |
| 脚本 | `scripts/orchestrate/pipeline.py` | **8 步闭环状态机 + 断点续跑**（第 7/8 步留 hook） | Ch1 §D.1 | C |
| 脚本 | `scripts/trace/traceback.py` | 四要素结构化反查 + 覆盖率自检 | Ch1 §E | C |
| 脚本 | `scripts/tasks/gap_to_task.py` | 缺口→任务幂等入队 | Ch1 §C.3 | C |
| 检查器 | `scripts/checks/no_signal_day.py` | 反 KPI 校验（无变化日 / `change_reason`） | Ch1 §F | C |
| 配置 | `rules/pipeline.yaml`（只读 0444）· `rules/review.yaml` · `audit/rule_changes.jsonl` | 8 步顺序 / 反拖延阈值口径 / 规则变更审计 | Ch1 §B·§F | B |
| 配置 | `registry/delivery.yaml` | **五阶段路线 + 可测通过判据 + 顺序锁定** | Ch11 §A.4 | B |
| 配置 | `rules/freeze.yaml` | **11 项 `freeze_param`**（`tbd` 占位 + `option_set` + `suggested_value`） | Ch11 §C/§D | B |
| 配置 | `registry/launch_criteria.yaml` | 首版启动条件（`required` 仅公开信息 / `optional` 付费可后置） | Ch11 §E.2 | B |
| 检查器 | `scripts/checks/freeze_guard.py` | 参数状态断言 + 红线"不补门槛"扫描 + 切换原子性 | Ch11 §E.1/§G | C |
| 检查器 | `scripts/checks/launch_guard.py` | 付费非首版条件守卫（含 D-01 `cost=0` 校验） | Ch11 §E.2 | C |
| 脚本 | `scripts/delivery/stage_gate.py` | **阶段判据守卫**（未过 → 阶段阻塞） | Ch11 §B | C |
| Skill | `skills/`（来源登记等六阶段各一个 `SKILL.md`） | 模块 I/O 契约载体 | Ch9 §3.2.1 | B |

**通过判据（可执行）**：

```python
def stage_prep_passed(cfg) -> bool:
    checks = {
        "four_deliverables": has_all_four_deliverables(cfg),          # 四类交付物齐备
        "rule_vs_impl_split": rules_and_impl_explicitly_separated(cfg), # 显式区分（可逐条指认）
        "caliber_tbd": all(p.freeze_status == "tbd" for p in cfg.freeze_params),  # 口径 tbd 占位
        "no_paid_required": not any(c.requires_paid_subscription for c in cfg.launch_criteria.required),
    }
    return all(checks.values())
```

**退出物（冻结）**：`registry/sources.yaml` + 口径表（`freeze_status=tbd`）

---

### 阶段② `nvidia_sample` NVIDIA 深度样例

> **进入条件**：阶段① 通过 ｜ **通过判据**：六步判断链完整（Ch1 五问 + Ch10 六结果）+ 证据可定位（Ch6 §D）+ 推导可复查；满足 **Ch4 §G** 深度 ｜ **退出物**：NVIDIA 底稿（**深度标准**，承 `10/01 §N10.1-03`）

**要建什么**：

| 类型 | 名称 / 落位 | 作用 | 出处 | 口径 |
|---|---|---|---|---|
| 数据 | `facts/businesses.jsonl`（业务单元 + 赚钱机制） | 谁付费 + 转换链路各段 + 每段证据 | Ch4 §B | B |
| 数据 | `facts/drivers.jsonl`（驱动 + 财务连接 + 增长代价） | 驱动→科目→每股指标三层映射 | Ch4 §D | B |
| 配置 | `rules/metric-sets.yaml` | 指标集注册表（硬件六段 / 云五段 / 软件五段 + 路由） | Ch4 §C | B |
| 配置 | `rules/baseline.yaml` | 上限/阈值参数化（`max_primary_drivers_per_business` / `min_nonnull_rate`） | Ch4 §B/§G | B |
| 脚本 | `scripts/valuelayer/route_guard.py` | 指标集绑定**防串味**（`MetricSetMismatch`） | Ch4 §C | C |
| 脚本 | `scripts/valuelayer/rollup.py` | 分部→公司层加总（不跨业务混用语义） | Ch4 §C | C |
| 脚本 | `scripts/valuelayer/growth_quality.py` | 增长质量 / ROIIC / 代价五类 + 收入增速 denylist | Ch4 §D | C |
| 检查器 | `scripts/valuelayer/moat_guard.py` | **护城河反误判** + 六保护对象 + 四类区分 | Ch4 §F | C |
| 检查器 | 护城河/价格**硬隔离** + 断言（A1/A2/A3） | schema 层物理隔离行情 | Ch4 §F.3 | C |
| 脚本 | `scripts/valuelayer/state_machine.py` | 3×4 状态机 + 增量更新 + 无变化分支 | Ch4 §E | C |
| 检查器 | `scripts/valuelayer/completeness.py` | 形式完备性校验器 + gap 衔接 | Ch4 §G | C |
| 脚本 | `scripts/pricelayer/solver.py` + `facts/implied_requirements.jsonl` | **反向求解**：固定 4 类解第 5 类，产出解集 + 区间 + 替代解释 | Ch5 §B | C |
| 脚本 | `scripts/pricelayer/valuation.py` + `rules/valuation-methods.yaml` | 估值计算 + 方法按 `model_class` 路由 + `DerivedValue` 追溯链 | Ch5 §D | C |
| 检查器 | `scripts/pricelayer/order_guard.py` | 顺序约束 + **倒填检测四规则** | Ch5 §D | C |
| 配置/脚本 | `rules/scenario.yaml` + `scripts/pricelayer/scenario_guard.py` | 情景一致性 + `probability` 默认 null | Ch5 §E | B+C |
| 检查器 | `scripts/pricelayer/history_guard.py` | AGIX 底稿 + 非上市资产 + 历史外推检测 | Ch5 §E | C |
| 脚本 | `scripts/pricelayer/daily_explain.py` | 每日解释：行情口径校验 + 三分类 + 因果链 | Ch5 §F | C |
| 脚本 | `scripts/compute/`（core / growth / margin / returns / fx / shares / valuation） | **确定性计算层（全包最重自建块）** | Ch9 §3.2.1 | C |
| Skill | `aichain-valuation-baseline` · `aichain-price-expectation` | 研究底稿（六项）生成 / 假设组合生成编排 | Ch4 §I · Ch5 §C | B |
| 数据 | `facts/baselines` | 研究基线 | Ch9 §N9.1-17/18 | B |

**通过判据**：六步链完整（Ch1 §D 五问 + `10/01 §N10.1` 六结果）+ 每条结论证据可定位（Ch6 §D）+ 推导可复查（`traceback_coverage == 1.0`）+ 满足 Ch4 §G 六项底稿深度。

---

### 阶段③ `core_chain` 核心产业链运行

> **进入条件**：阶段② 通过 ｜ **通过判据**：多公司传导跑通（Ch7 §C.7）+ **T01–T14 全过**（`10/01 §2.1`）+ 图谱/提问任务可追溯 ｜ **退出物**：多公司样例 + `acceptance_input_set`（首批名单 + 样本事件）

**要建什么**：

| 类型 | 名称 / 落位 | 作用 | 出处 | 口径 |
|---|---|---|---|---|
| Skill | `skills/aichain-claim-extract` | 多源采集编排（含失败降级 / 备选切换） | Ch6 §D | B |
| 脚本 | `scripts/graph/independence.py` + `facts/claim_propagation.jsonl` | 去重 / 传播 / 独立判定 | Ch6 §C | C |
| 脚本 | `scripts/decision/triage.py` | **三维决策函数（序 / 信 / 深）+ 反加权测试** | Ch6 §B | C |
| 脚本 | `scripts/claim/transition.py` | 五态状态机 + `superseded` 传播 | Ch6 §E | C |
| 配置 | `registry/publisher-entities.yaml` + `rules/tiering.vN.yaml` | publisher-entity 名录 + 分级规则 | Ch6 §B | B |
| 检查器 | `scripts/validators/locator_check.py` | `full_text_read` 校验器（程序拦截，非模型自觉） | Ch6 §D | C |
| 脚本 | `scripts/ingest/transcript.py` | 字幕/视频处理（降级路径，公开字幕优先） | Ch6 §D | C |
| 配置 | `rules/budget.yaml` | 三类预算闸门（时间 / 计算 / 检索） | Ch6 §D.5 | B |
| 配置 | `facts/relation_flows.jsonl` · `relations` 增量 schema · `facts/businesses.jsonl`（增量） | 关系/三边/业务 schema + 9 类映射 | Ch7 §B | B |
| 脚本 | `scripts/transmit/engine.py` + `stop.py` | **传导引擎编排 + 停止判定（7 条件）** | Ch7 §C | C |
| 脚本 | `scripts/transmit/cycles.py` + `demand.py` | **环检测 SCC** + 终端需求归因 + 累计上限 | Ch7 §C | C |
| 检查器 | `scripts/transmit/stage_guard.py`（+ `stop.evidence_gate`） | 依据门槛 + 禁跳级等同（`capex` ≠ 订单/收入） | Ch7 §C | C |
| 脚本 | `scripts/decision/rules.py` + `gate.py` | **建议决策引擎（5 规则 + 前置门 + `DerivedValue` 强制）** | Ch7 §D | C |
| 脚本 | `recommendations.status` + `check_record` + `judgment_change` | `boundary_unresolved` + 核查记录 + 判断变化检测 | Ch7 §D | C |
| 视图 | `views/`（`recommendations` 8 字段视图） | 建议展示（含催化剂语义） | Ch7 §E | B |
| 脚本 | `index/adjacency.sqlite` | 可重建邻接表缓存（**非真源**） | Ch7 §C | C |
| 脚本 | `scripts/graph/`（遍历 / T12 传播） | 依赖图遍历与传播 | Ch9 §3.2.1 | C |
| 测试 | `tests/test_transmission_capex.py` · `tests/test_gate_reject_raw_number.py` · `tests/test_no_relation_contagion.py` | 传导 / 决策单测 + capex 4 跳用例 + 防传染断言 | Ch7 §I | C |

**通过判据（可执行）**：多公司传导跑通 + **T01–T14 逐项通过/失败记录齐备，任一 ❌ 即整体不通过**。

---

### 阶段④ `daily_run` 每日持续研究

> **进入条件**：阶段③ 通过 ｜ **通过判据**：连续运行下**时点可核**（Ch9 §2.2）+ **覆盖可核** + **任务状态可核**（Ch8 §C.2）；**降级保留上次有效结果** ｜ **退出物**：`state.json` 游标 + 运行日志

**要建什么**：

| 类型 | 名称 / 落位 | 作用 | 出处 | 口径 |
|---|---|---|---|---|
| 配置 | `facts/tasks` 5 字段 + 五态状态机 + `parent_context` | 任务契约 | Ch8 §C | B |
| Skill | `skills/aichain-ask` + `scripts/tasks/ask_to_task.py` | 提问入口（同流程 + 三件输出） | Ch8 §C | B |
| 配置 | `rules/trigger.yaml` · `rules/schedule.yaml` + Automation | 调度契约（五类触发 + 四时间戳时序） | Ch8 §D | A/B |
| 检查器 | `scripts/checks/degrade_write_guard.py` + `stale_since` + 三级异常态渲染 | **降级契约**（不置 null / 不标最新） | Ch8 §E | C |
| 测试 | `tests/test_ch8_invariants.py` | 幂等与增量不变量 | Ch8 §G | C |
| 视图 | `views/graph/` · `views/company/` · `views/daily/` | **三个阅读入口** | Ch8 §B | B |
| 检查器 | `scripts/views/neutrality_check.py`（**扩项 S-02/03/06/08**） | 展示层守卫扩项 | Ch8 §G | C |
| 配置 | `rules/notification.yaml` · `rules/publish.yaml`（+ 前端 `animation_policy`） | 通知/发布配置（**接口先定、值 `tbd`**） | Ch8 §K ③ | B |
| Skill | `skills/aichain-daily` | 每日运行编排 | Ch8 §H | B |
| 脚本 | `scripts/ops/`（含 `ops/diagnose.py <task_id>`） | 故障定位 / 备份 / 费用（**运维五交付物**） | Ch9 §3.6 | C |

**通过判据（可执行）**：

```python
def stage_daily_run_passed(run_window) -> bool:
    checks = {
        "timing_replayable": all_events_have_four_timestamps(run_window),      # Ch9 §2.2
        "coverage_verifiable": coverage_matches_acceptance_input(run_window),  # 10/01 §N10.1-06
        "task_state_auditable": all_tasks_have_status(run_window),             # Ch8 §C.2
        "degrade_keeps_last_valid": no_null_overwrite_after_failure(run_window), # Ch8 §E
    }
    return all(checks.values())   # 任一 False → 阶段阻塞
```

**退出物**：`state.json` 游标 + 运行日志

---

### 阶段⑤ `expansion` 覆盖扩展与评估

> **进入条件**：阶段④ 通过 ｜ **通过判据**：研究标准**保持一致**（Ch4 §G）+ 投资结果**可核验**（Ch10 §N10.3-04）；复盘 **append-only** ｜ **退出物**：三层复盘记录

**要建什么**：

| 类型 | 名称 / 落位 | 作用 | 出处 | 口径 |
|---|---|---|---|---|
| 契约 | 六结果追溯契约（`six_results_ref` + 覆盖率 + 双版本 diff），落 `views/` | 六环可下钻、覆盖率 = 1.0 | Ch10 §B | B |
| 脚本 | `scripts/verify/research_complete_gate.py` + 反 KPI 断言 S-01 | **复用 Ch7 §D `gate`，零新增门槛** | Ch10 §B.4 | B |
| 字段 | `tasks.eval_result` +3 字段（`eval_layer` / `error_axis` / `error_axis_note`） | 三层复盘结构记录、append-only | Ch9 §3.3.5 · Ch10 §C.1 | B |
| 脚本 | `scripts/review/record_eval.py` | 三层复盘记录（append-only） | Ch10 §C | B |
| 脚本 | `scripts/review/review_return.py` | 复盘收益（过 `return_guard`，口径经 `get_param('p10')`） | Ch10 §D | C |
| 脚本 | `scripts/review/capability_source.py` | `assert_capability_from_forward_only` / `assert_forward_not_backfilled` | Ch10 §E | C |
| 脚本 | `scripts/trace/traceback_coverage.py` | `six_results_complete` / `traceback_coverage_ok` | Ch10 §B | C |
| 检查器 | `scripts/checks/error_axis_guard.py`（**S-07 新建**） | 标记字段**不进决策函数** | Ch10 §G | C |
| 配置 | `registry/acceptance_input_set = {first_list_version, sample_event_versions[]}` | 验收输入清单 | Ch10 §A | B |
| 测试 | `tests/test_acceptance_*.py`（T01–T14 逐项）+ `tests/test_ch10_invariants.py` + 人工核对清单 | 逐项通过/失败记录 | Ch10 §I | C |
| 视图 | 复盘/验收展示视图（双版本并排 + 三层面板 + 接入 `neutrality_check.py`） | 展示层中立 | Ch10 §H | B |
| 数据 | `snapshots/`（带资料日期与版本）+ 三层权限白名单 | 分享快照 | Ch9 §N9.2-07 | B |

**通过判据**：研究标准一致（Ch4 §G）+ 投资结果可核验（前向记录）+ 复盘 append-only（`(success, failure, pending)` ⊆ 记录集，禁选择性删除）。

---

## 3. 全局组件总表（按类型去重）

### 3.1 新增配置（`rules/` · `registry/`）

| 落位 | 作用 | 阶段 | 出处 |
|---|---|---|---|
| `rules/banned_tokens.yaml` | 禁词表（全包唯一禁词源，token 级 `scope`） | ① | Ch2 §B.2 |
| `rules/pipeline.yaml` | 8 步顺序 / 每步产物 / 是否阻塞（只读 0444） | ① | Ch1 §B |
| `rules/review.yaml` | 反拖延 / 反造信号阈值口径 | ① | Ch1 §F |
| `rules/benchmark.yaml` | 基准对象声明 | ① | Ch3 §B-17 |
| `rules/scope.yaml` | 推荐范围声明（`recommended_security_scope`） | ① | Ch3 §B-17 |
| `registry/delivery.yaml` | 五阶段路线 + 可测判据 + 顺序锁定 | ① | Ch11 §A.4 |
| `rules/freeze.yaml` | 11 项 `freeze_param`（**参数唯一真源**） | ① | Ch11 §C/§D |
| `registry/launch_criteria.yaml` | 首版启动条件（`required` / `optional`） | ① | Ch11 §E.2 |
| `rules/metric-sets.yaml` | 指标集注册表 + 路由 | ② | Ch4 §C |
| `rules/baseline.yaml` | 底稿上限/阈值参数化 | ② | Ch4 §B/§G |
| `rules/valuation-methods.yaml` | 估值方法路由表 | ② | Ch5 §D |
| `rules/scenario.yaml` | 情景定义 + `scenario_method_status` | ② | Ch5 §E |
| `registry/publisher-entities.yaml` | publisher-entity 名录 | ③ | Ch6 §B |
| `rules/tiering.vN.yaml` | 来源分级规则（版本化） | ③ | Ch6 §B |
| `rules/budget.yaml` | 三类预算闸门 | ③ | Ch6 §D.5 |
| `rules/trigger.yaml` | 五类触发契约 | ④ | Ch8 §D |
| `rules/schedule.yaml` | 调度时刻 / 时区（p07 指向，不另存值） | ④ | Ch8 §D |
| `rules/notification.yaml` | 通知渠道与聚合（值 `tbd`） | ④ | Ch8 §K ③ |
| `rules/publish.yaml` | 发布策略（值 `tbd`） | ④ | Ch8 §K ③ |
| `registry/acceptance_input_set` | 验收输入清单 | ⑤ | Ch10 §A |
| `registry/data-sources.allowlist.yaml` | 数据源许可（D-01：`license_tier=free_quota` / `cost=0`） | ① | Ch9 §3.6 · D-01 |

> **单一真源纪律**：参数值唯一真源 = `rules/freeze.yaml::freeze_param`；`rules/schedule.yaml`（p07）、`data-sources.allowlist.yaml`（p09）由对应 `freeze_param` **指向**，**不另存第二份值**（防双写漂移，`11/02 §D.2`）。

### 3.2 新增脚本 / 检查器（`scripts/`）

| 分类 | 清单 | 阶段 |
|---|---|---|
| **计算（C，最重）** | `scripts/compute/`（core/growth/margin/returns/fx/shares/valuation） | ② |
| **估值（C）** | `scripts/pricelayer/`（solver / valuation / order_guard / scenario_guard / history_guard / daily_explain） | ② |
| **价值层（C）** | `scripts/valuelayer/`（route_guard / rollup / growth_quality / moat_guard / state_machine / completeness） | ② |
| **图谱（C）** | `scripts/graph/` · `scripts/graph/independence.py` | ②–③ |
| **传导（C）** | `scripts/transmit/`（engine / stop / cycles / demand / stage_guard） | ③ |
| **决策（C）** | `scripts/decision/`（gate / rules / triage） | ③ |
| **主张（C）** | `scripts/claim/transition.py` · `scripts/validators/locator_check.py` | ③ |
| **编排（C）** | `scripts/orchestrate/pipeline.py` · `scripts/trace/traceback.py` · `scripts/tasks/gap_to_task.py` | ① |
| **每日运行（C）** | `scripts/checks/degrade_write_guard.py` | ④ |
| **运维（C）** | `scripts/ops/`（含 `ops/diagnose.py <task_id>`） | ④ |
| **验收复盘（C）** | `scripts/verify/research_complete_gate.py` · `scripts/review/`（record_eval / review_return / capability_source） · `scripts/trace/traceback_coverage.py` | ⑤ |
| **检查器（C，命中即 fail）** | `conflict_scan.py` · `assert_gate_input.py` · `module_denylist.py` · `no_signal_day.py` · `freeze_guard.py` · `launch_guard.py` · `error_axis_guard.py` · `scripts/views/neutrality_check.py` | ①–⑤ |
| **守卫（C）** | `scripts/benchmark/return_guard.py` · `scripts/scope/anti_padding.py` · `scripts/delivery/stage_gate.py` | ① |
| **采集（C）** | `scripts/ingest/`（含 `transcript.py`） | ①·③ |

### 3.3 新增视图与 Skill

| 类型 | 清单 | 落位 | 阶段 |
|---|---|---|---|
| 视图 | 三入口：图谱页 / 公司页 / 每日页 | `views/graph/` · `views/company/` · `views/daily/` | ④ |
| 视图 | 建议 8 字段视图 | `views/` | ③ |
| 视图 | 交付阶段看板（逐阶段"通过/未通过"） | `views/delivery/` | ①–⑤ |
| 视图 | 复盘/验收视图（双版本并排 + 三层面板） | `views/` | ⑤ |
| Skill | 六阶段各一个 `SKILL.md` | `skills/` | ①起 |
| Skill | `aichain-claim-extract` · `aichain-price-expectation` · `aichain-valuation-baseline` · `aichain-ask` · `aichain-daily` · `aichain-delivery` | `skills/` | ②–④ |

### 3.4 测试资产

| 落位 | 内容 | 阶段 |
|---|---|---|
| `tests/conflict/test_schema_no_stop.py` · `test_schema_no_aggregate.py` | schema 禁止字段断言（P-03/04/05/07） | ① |
| `tests/test_ch8_invariants.py` | 幂等与增量不变量 | ④ |
| `tests/test_ch11_invariants.py` | 参数冻结单调 + 切换原子 + 付费非首版 | ① |
| `tests/test_transmission_capex.py` 等 3 套 | 传导 / 决策 / 防传染单测 | ③ |
| `tests/test_acceptance_*.py` + `tests/test_ch10_invariants.py` | **T01–T14 逐项** + 三层不变量 | ⑤ |
| `tests/`（注入测试 **10 用例**：忽略规则买入 / 换基准 / 删反证 / 改规则文件 / base64 藏指令 / 藏表格图片字幕 / 诱导 shell / 伪造分隔符 / 同形字 / 自称官方提权） | 注入防护 | ① |

---

## 4. 目录地基（`09 §3.3.3` 逐字）

> **工程根 = `/Users/gaza/Developer/InvestSigh/`（macOS），本身是一个 Git 仓库。**

```
/Users/gaza/Developer/InvestSigh/
├── rules/      只读规则（第二章冻结的投资规则，模型不可写）
├── registry/   来源登记表 + 公司行动表 + 人工质量标签
├── raw/        原始物（页面快照、PDF、字幕、合法保留片段）
├── facts/      18 个追加式 JSONL（8 类对象唯一真源）
├── derived/    确定性计算结果（带公式与操作数引用）
├── index/      可重建 SQLite 索引
├── snapshots/  分享快照（带资料日期与版本）
├── views/      HTML 展示层（渲染物，非事实源）
├── scripts/    compute / graph / bitemporal / guard / ingest / ops
├── skills/     六阶段各一个 SKILL.md
└── tests/      单元测试与注入测试用例
```

**`facts/` 的 18 个 JSONL → 8 类对象**：

| JSONL | 承载对象 |
|---|---|
| `industry_nodes` · `companies` · `securities` · `business_positions` | 公司与证券（含多业务位置） |
| `products` · `relations` | 产品与关系 |
| `sources` · `claims` · `claim_propagation` | 来源与主张（含引用链、传播记录） |
| `events` · `impacts` | 事件与影响 |
| `baselines` | 研究基线 |
| `prices` · `expectations` | 价格与预期 |
| `benchmarks` · `recommendations` | 基准与建议 |
| `tasks` | 任务与复盘 |
| `dependency_edges` | 依赖图边（支撑 T12 传播） |

---

## 5. 关键路径与并行机会

### 5.1 硬前置链（必须串行）

```
Ch9 目录地基 + facts schema  ──▶  Ch2 规则与检查器  ──▶  阶段① 通过
        │
        └──▶  Ch9 compute  ──▶  阶段②（NVIDIA 深度样例）
                │
                └──▶  Ch9 graph + Ch6 证据层  ──▶  阶段③（多公司传导）
                        │
                        └──▶  Ch9 ops + Automation  ──▶  阶段④（每日运行）
                                │
                                └──▶  Ch9 快照/权限 + Ch10 验收复盘  ──▶  阶段⑤
```

### 5.2 可并行（SubAgent，免费加速）

| 可并行组 | 说明 |
|---|---|
| 阶段① 内：Ch2 检查器 ∥ Ch3 基准与范围 ∥ Ch11 参数配置 | 三者无相互依赖，可同时开工 |
| 阶段② 内：Ch4 价值层 ∥ Ch5 价格层 | 两侧都读 Ch9 基座，产出互不阻塞（Ch5 §A.1 只读 Ch4 的 `driver_model` 等接口，接口先冻结即可并行） |
| 阶段③ 内：Ch6 证据层 ∥ Ch7 传导层 | Ch7 依赖 Ch6 的 `claims` 字段口径，**接口先冻结**即可并行 |
| 各阶段内：测试与文档 | 与实现并行 |

### 5.3 外部依赖（不可自行消化）

| 依赖 | 说明 | 影响 |
|---|---|---|
| **A 档 6 项参数拍板** | 主基准 / 复盘口径 / 建议范围 / 发布复核 / 负收益处置 / 通知分享 | **阻塞验收，不阻塞开工**（`tbd` 可切换） |
| **Ch10 ↔ Ch11 同批交付** | 复盘口径 `p10` 是 Ch10 §N10.3-04/08/11 的**唯一输入** | 分开做**必然口径不一致**（承 C10-2 / C11-4） |
| **技术待定项 J1–J11**（Ch9 §3.10） | 存储组合 / 备份位置 / 运行时刻 / 模型分离 / as-of 性能 / 并发写 / 事件指纹 / 脱敏 / 规则权限 / RPO·RTO / 版本锁定 | J8 脱敏**待法务口径**；J3 运行时刻**待 Ch11 冻结** |
| **D-01 免费额度** | iFinD / pandadata 标 `license_tier=free_quota` / `cost=0`，**可进首版清单但不得写入裁决逻辑** | 若转付费按 Ch2 §N2.1-14 重评 |

---

## 6. 工作量汇总

### 6.1 分章开发成本（1 人日 = 熟练工程师专注 6h）

| 章 | 人日 | 来源 |
|---|---|---|
| Ch1 + Ch2 + Ch3 | 8–14 | ⚠️ **无分章出处**——三章 02 **均未设**「成本与复杂度增量」节；该值仅见于 `08/02 §J.1` / `10/02 §J.1` / `11/02 §J.1` 的"全项目累计"**公式内** |
| Ch4 + Ch5 | **34–49** | **分章相加**：Ch4 `§J.3` 合计 **15–23**（`04/02`）+ Ch5 `§I.1` 合计 **19–26**（`05/02`） |
| Ch6 | 16–24 | `06/02 §J.1` |
| Ch7 | 19–25 | `07/02 §I.1` |
| Ch8 | 13.5–18.5 | `08/02 §J.1` |
| Ch9 | 42–55 | `09 §3.8.1`（**最重**） |
| Ch10 | 11.5–16.5 | `10/02 §J.1` |
| Ch11 | 9–13.5 | `11/02 §J.1` |
| **合计（分章逐项相加）** | **153–215.5** | —— |

> **口径注（如实登记：共 3 处原文差异，未消除、供追溯）**：
> ① **累计式互不相等**——`11/02 §J.1` 的"全项目累计" = **142–195 人日**（该式**未含 Ch10**）；`10/02 §J.1` = **145–198 人日**。两者差 **3**。
> ② **Ch4/Ch5 口径不一致**——上述两式均写 **34–45**，但两章**分章相加实为 34–49**（Ch4 15–23 + Ch5 19–26），差 **4**。
> ③ **Ch1/2/3 的 8–14 无分章出处**——三章无成本节，该值只存在于累计公式内部，**无法逐项复核**。
> **本表口径 = 分章逐项相加（含 Ch10）= 153–215.5**；原文累计式 = 142–195（Ch11）/ 145–198（Ch10）。**排期以本表为准，三处差异视为待消除项（见 §7 N-02）。**
> **口径边界**：以上均指"跑通 NVIDIA 深度样例 + 一条多公司传导 + 每日连续运行"，**不含** A 档拍板后的参数校准工时。

### 6.2 复杂度评级

| 维度 | 评级 | 原因 |
|---|---|---|
| 技术复杂度 | **中–高** | 无新算法，但确定性计算层 / 传导图算法 / 估值反解必须自建且可单测 |
| 运维复杂度 | **中** | 复用同一 Automation / ops；新增检查器多但均为纯函数 |
| 数据治理复杂度 | **高** | 8 类对象 / 18 JSONL / 双时间轴 / 参数控制面，均需与 `00_待拍板项清单` 逐条对齐 |

---

## 7. 施工前待核 / 前置阻塞

| # | 事项 | 现状 | 建议 |
|---|---|---|---|
| **N-01** | **`10/02` 内 `T01–T14` 权威表指向不一致** | `10/02 §I.2` 的 N10.2-02 行写"**本章 §B.1** 为 T 唯一权威出处"；但 `10/02 §B.1` 实为「**六结果链条表**」，与 T 表无关。`10/02` 文末自注与 `10/01 §2.1` 标题均指向 **`10/01 §2.1`**（唯一权威表） | **施工查表一律以 `10/01 §2.1` 为准**；建议修正 `10/02 §I.2` 该行（**待需求方确认**） |
| **N-02** | **人日口径共 3 处差异**：① 两处累计式互不相等（142–195 vs 145–198，差 3）；② Ch4/Ch5 在累计式内写 34–45，但分章相加实为 **34–49**（差 4）；③ Ch1/2/3 的 8–14 **无分章出处**（三章无成本节） | 见 §6.1 口径注；本图口径 = **分章逐项相加 = 153–215.5** | 未消除，标注供追溯；**排期以 §6.1 逐项相加为准**，差异作待消除项 |
| **N-03** | A 档 6 项参数未拍板 | `freeze_status = tbd`，机制就绪 | 不阻塞开工；**验收前须拍板** |
| **N-04** | J8 脱敏清单待法务 | Ch9 §3.10 J8 | 交付日前须获口径 |
| **N-05** | J3 运行时刻/时区 | Ch9 §3.10 J3 = Ch11 p07 | 随阶段④前置冻结 |

---

## 8. 施工纪律速查（硬约束）

| # | 纪律 | 出处 |
|---|---|---|
| 1 | **零新增门槛**——参数只做"基准/范围/口径/披露"，**不进决策函数** | Ch2 §B.2 · N11.2-12 |
| 2 | **命中即 fail，禁 warn-only**——所有扫描/断言非零退出 | Ch2 §B.3 |
| 3 | **禁词入库前必须 grep 同名 + 声明可执行 `scope`**（只消歧，不新增禁止项） | Ch2 §B.1 通则 |
| 4 | **追加式不可变**——pre-commit 拒改既有行（新增放行） | Ch9 §3.4.2 |
| 5 | **参数值唯一真源 = `rules/freeze.yaml`**，其余配置只指针、不双写 | Ch11 §D.2 |
| 6 | **引用一律节号锚点，禁绝对行号** | 全包 |
| 7 | **枚举 token 英文小写 + 下划线**；"待定"统一 `tbd` | Ch2 §C.2 |
| 8 | **同名不同域必须消歧**（`tier` / `license_tier` / `param_tier`；`stage` / `delivery_stage`） | Ch2 §C.2 |
| 9 | **单写者 + 文件锁**（首版）；`rules/` 0444 + SHA256 | Ch9 §3.10 J6 / §3.4.11 |
| 10 | **模型对 `rules/` 无写权**（硬要求） | Ch9 §3.10 J9 |
| 11 | **复用优先**：能 A 不 B，能 B 不 C；C 档只保留可单测薄层 | Ch9 §3.2 |
| 12 | **阶段未过即阻塞，不得静默跳过** | Ch11 §B |

---

## 附：回改记录（v1.1 · 2026-09-15）

| R号 | 内容 | 对应需求 | 关联冲突 |
|---|---|---|---|
| R-35 | **新建本文件**（根级 `00_交付施工图.md`）：把 11 章成果收敛为五阶段施工路径（交付物 / 依赖 / 验收点 / WorkBuddy 口径 A·B·C），并登记施工前待核项 N-01~N-05 | 交付实施准备（收口后新增件） | N-01（T 表指向）；`02/02 §F` R-35 |
| R-37 | **运行环境 Windows → macOS，工程根路径同步**：① §2 阶段① 骨架行 → `/Users/gaza/Developer/InvestSigh/`；② §4 工程根声明 → `/Users/gaza/Developer/InvestSigh/`（macOS）+ 目录树首行同步。**未新增门槛、未改策略口径**（同步 `00_开发Agent开工提示词.md` / `09 §3.3.3`） | 交付实施准备（环境迁移） | 无 |
